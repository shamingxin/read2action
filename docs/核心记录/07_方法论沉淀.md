# Read2Action · 07 方法论沉淀

> **用途**：沉淀 **走查方法、问题分级、验收原则、文档与实现对齐、作品集叙事** 等可复用方法；便于后续 **1.1 立项** 与 **作品集 / 面试讲解** 直接引用。  
> **合并来源**：原 `Read2Action_9.3_问题记录表.md` 全文、`Read2Action_Cursor_新对话接力文档` §6–§7（Figma / 已知问题）、`Read2Action_决策日志.md` 中范围决策与复盘语句、`Read2Action_发给Cursor材料清单与操作步骤.md` 中关于分阶段与「不要一次做完」的实践、`Read2Action_Cursor开发交接总文档` 中阶段摘要；**以及经 ChatGPT 与用户对齐确认的《Read2Action 1.0 复盘内容》要点（见 §8，非原文照录；一页式归档另见 `docs/阶段复盘/Read2Action_1.0_复盘总结.md`）**；**1.1** 上线收尾与 **P0** 备忘见 **§2.6** 与 **`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**。**9.3 走查表全文**已迁至 **`docs/阶段复盘/Read2Action_1.0_第九阶段走查与问题闭环实录.md`**（`07` 不重复粘贴）。  
> **说明**：原独立文件 `Read2Action_方法论沉淀.md` 在旧材料清单中被提及但 **仓库历史中未必存在独立稿** — 本文档作为 **正式方法论载体** 新建并收拢上述内容；**长过程**放 **`docs/阶段复盘/`**，`07` 只保留 **提纲与索引**（见 **`docs/README.md`** 整理原则）。

---

## 1. 核心方法论总览（一句话）

**先用 mock + 分阶段交付跑通主链路，再以走查表 + 严重程度分级收口；文档与浏览器事实同源，旧规格文档降级为「对照参考」而非唯一真理。**

---

## 2. 分阶段交付与「不要一次做完」

### 2.1 为何分阶段

- **降低上下文丢失风险**：每阶段文件与验收边界清晰，便于 Cursor 新对话接力。  
- **控制返工成本**：先结构后视觉，先主链路后占位，避免一上来接后端/AI 导致全栈纠缠。  
- **对齐 Figma 成本**：高保真 6 页分步落地，比「一次生成全站」更易对齐组件与 token。

### 2.2 实践要点（来自材料清单与项目实际）

- 每一阶段结束时要求 Agent **列出改动文件、测试方式、遗留问题**。  
- **禁止**单次对话「把整个项目做完」的指令风格。  
- **1.0 完成后**：进入 **1.1 准备期** 时反过来 **冻结代码**，先对齐接口与立项 — 这是 **另一种分阶段**（方案先行）。

### 2.3 1.1 接力经验：服务端 API 先独立验证，再接前端页面

- **做法**：在 **`08`** 契约对齐后，**先**实现 **`POST /api/analyze`** 等 **Route Handler**，用 **`curl` / Postman** 在本地或预览环境 **独立打通**模型与 JSON 映射；**再**进入 **`/parsing` / `/result`** 接线。  
- **收益**：密钥与上游错误在 **小范围** 内暴露、易排查；**避免**「页面 + 动画 + 路由 + AI」同时失败时难以归因；符合 **密钥仅服务端** 的安全习惯。  
- **配套**：**`.env.local` 不入库**；**`.env.example` + README** 写清变量名；**每小步列文件清单并经确认后再写代码**，防止 scope creep。

### 2.4 1.1 真实 AI 接入的工程纪律：防重、超时、取消、费用与 Network 验证

- **请求防重**：前端 **`/parsing`** 等页面 **不得**把 **`POST /api/analyze`** 绑在会因 **`setState` / `router` 引用变化** 而反复触发的 **`useEffect` 依赖链**上；应 **单次挂载启动 + 世代门闩（如 `commitRef`）+ `inFlightRef`**，并 **兼容 React StrictMode**（dev 下 effect 双跑 **不得**变成双计费）。**禁止**自动无限重试；**仅用户显式「重试」** 才发新请求。  
- **超时**：客户端 **`fetch` 须有上限**（本项目 **`postAnalyze` 默认 30s**），超时映射为可读错误，**避免**长挂连接与不可控账单。  
- **取消**：**`AbortController.abort()`** 与 **清定时器** 必须同时到位；取消后 **不得**再跳转或写成功态；**pending 正文 session key** 须在 **成功 / 失败 / 取消** 等终态清理，避免再次进入 **`/parsing`** 自动重打 API。  
- **费用控制意识**：真实供应商 **按次 / 按 token** 计费；P0 级事故多为 **循环请求** — 接 AI 第一天就要把 **「单次用户动作 → 最多 N 次上游调用」** 写进验收与自测。  
- **Network 先于盲测真 API**：先用 **`npm run test`（mock `fetch`）** 或本地代理确认 **计数**，再用浏览器 **DevTools → Network** 确认 **单次主路径仅 1 条 `POST /api/analyze`（未点重试）**，**然后** 才做短时、可监督的真 API 抽检；**禁止**未验证即对生产供应商做无监督、长时间或脚本化压测。  
- **本地 dev 与 Next 16（Read2Action 实录）**：**`next dev` 默认可能走 Turbopack**；若出现 **Turbopack FATAL** 或 dev **不稳定**，优先以 **`next dev --webpack`** 作为 **复验与排障基线**（**`package.json`** **`scripts.dev`**），并 **清理 `.next`、避免多进程占同一端口**，再判断是否为上游 API 问题。结论与验收记录见 **`06` 2026-05-20**、**`04` 2026-05-20**。

### 2.5 1.1 P0 排障时间线实录（接力节点 · 2026-05-21）

> **详版叙述与人工复验数字**（含 **~6.37s**、Vitest / `next build` 备注）：见 **`docs/阶段复盘/Read2Action_1.1_P0_重复请求排障与验收实录.md`**；决策节点见 **`docs/核心记录/04_重要决策日志.md` 2026-05-21**；版本线见 **`docs/核心记录/06_版本更新记录.md` 2026-05-21**。

| 顺序 | 现象 | 说明 |
|------|------|------|
| 1 | **0** 条 **`POST /api/analyze`** | **`pendingAnalyzeText`** 被提前清理；**`/parsing`** 一闪回首页 |
| 2 | **多条 POST** | **DeepSeek** 费用 **P0** |
| 3 | **单条 POST 长期 pending** | 与 **Turbopack FATAL**、终端反复 **`GET /parsing` 200** 叠加，易误判为上游 |
| 4 | **收束** | **保留 A+B+C**；**`dev` → `next dev --webpack`**；**清 `.next`、单进程 dev**；**Network** 复验 **1 条 / 200 / `/result` 真实 AI** |

**下一小步（文档状态）**：**1.1 第三小阶段 ✅ 已落地**（**`localStorage` + mock 合并**）— 见 **`02` §8.4**、**`03`**「**1.1 第三小阶段 · 交付收尾**」；**GitHub / Vercel 线上验收 ✅** — 见 **`04` 2026-05-13**、**`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**。**1.1 正式总复盘**见 **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`**；**完全阶段冻结**待审阅确认（当前：**功能与线上验收 ✅**）。**1.2** 项 **须单独立项**（见 **`03`** 后续规划备忘、**`04`**）。

---

### 2.6 1.1 第二小阶段 P0（`/parsing` 重复请求）· **总复盘必写专题**

> **背景**：**`/parsing`** 曾出现 **`POST /api/analyze` 重复请求** → **DeepSeek 等费用 P0**。**最终工程面**保留 **A+B+C** 防重复方案；**本地与线上**验收均以 **「单次用户主路径、未点重试 → 仅 1 条 POST」** 为硬标准。排障时间线见 **`docs/阶段复盘/Read2Action_1.1_P0_重复请求排障与验收实录.md`**；决策与里程碑见 **`06` 2026-05-19～05-21**、**`04` 2026-05-20 / 2026-05-21**。

**总复盘应覆盖的归因维度（备忘清单）** — **全文与展开**见 **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`** **§4～§5** 与 **`Read2Action_1.1_上线与验收收尾记录.md` §五**；此处仅列 **索引**：

1. **React StrictMode** 与 **`useEffect`** 双跑。  
2. **`cleanup` / `AbortController` / `inFlightRef`** 竞态。  
3. **`inFlightRef` / `commitRef`** vs **session 级锁（C）**。  
4. **定时器 / 阶段机 / 导航** 间接触发。  
5. **重挂载** 与 **世代门闩**。  
6. **A+B+C** 最终组合（以代码与 **`08` / `02` §8.3** 为准）。  
7. **Network** 硬验收标准。  
8. **测试债** 与后续绑定 **`npm run test`** 的纪律。

**与上线收尾文档的关系**：线上 **18** 项验收与 **P0** 提醒全文见 **`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md` §五**、**`04` 2026-05-13**。

---

### 2.7 1.2 记录生命周期与 docs 结构规范（2026-06-09）

> **详版**：**`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md`**；体验长文见 **`Read2Action_1.2_复盘总结.md`**。

**1.2 核心方法论**

- **先定义数据生命周期，再做功能页面** — 输入 → 解析中 → 生成结果 → 自动暂存 → 最近找回 → 保存到项目 → 项目内管理。  
- **AI 产品验全链路八项** — 输入真实 / 请求单次 / 处理稳定 / 输出可信 / 来源可追溯 / 可保存 / 可找回 / 可继续操作（见 SOP **§16.5**）。  
- **体验验收从「页面检查」升级为「路径检查」** — 新解析、返回结果页、最近找回、保存项目、项目内解析、详情页、刷新、宽窄屏。  
- **版本后期反新增功能** — 只处理 P0/P1；P2 及以下进后续问题池。

**docs 结构规范（正式结论）**

> Read2Action 后续 docs 采用「**PRD / 版本启动方案 / 阶段复盘 / 核心记录**」四类结构。后续版本只按既有目录新增或更新文档，不新增平行目录，不大规模重命名历史文件。

**AI 协作纪律（1.2 沉淀）**

1. **Cursor 分段执行** — 一次只解决一个问题；长对话新开 agent。  
2. **先只读检查，再修改** — 确认目录结构、文件命名、已有实现后再动手。  
3. **docs-only 更新必须明确禁止改功能代码** — 功能冻结后不得改 `app/`、`components/`、`lib/` 等。  
4. **版本收尾必须更新 docs** — 复盘、**`02`** 进度快照、**`03`** 任务清单、**`04`** 决策日志、**`06`** 版本记录、SOP。

---

### 2.8 1.3 / 1.4 版本路线与防自嗨检查（2026-06-09）

> **详版**：**`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md` §9～§10**；任务清单见 **`03` §0.1～0.3**。

**版本路线（当前共识）**

| 版本 | 定位 | 核心增量 |
|------|------|----------|
| **1.3** | 账号登录 + 数据库版 | localStorage → 账号 + 云端 DB；换设备找回；设计规范基线（1.3-0） |
| **1.4** | 文件上传 / 更多输入来源版 | 文件解析；Web H5 基础响应式；Web 端阶段性收工 |
| **1.5 / 2.0** | 暂不展开 | 根据真实使用再决定 |

**1.3 关键纪律**

- **先 PRD + 启动方案，再拆小步写代码**；当前仅规划方向。  
- **1.3-0**：`Read2Action_界面设计规范_v1.0.md` 沉淀于 **`docs/核心记录/`**（立项时创建）。  
- **不做大 UI 重构**；Cursor 改 UI 须先读设计规范；规范不替代人工验收。

**防自嗨检查**

> Read2Action 当前阶段的目标不是无限扩展成大型知识库、Notion、飞书或 Agent 平台，而是做成一个 **真实能用、能展示、能复盘、能放进作品集的完整 Web 工具**。**1.3 打基础，1.4 补输入能力，之后阶段性收工**，是当前更健康的版本策略。

---

## 3. Mock-first 与边界意识

### 3.1 Mock-first 路径

```text
mock data → local state → simulated parsing → 可部署演示 →（1.1）真实 API →（2.0）持久化与账号
```

### 3.2 边界写清「不接什么」

明确 **不做登录、不做真实数据库、不做真实 AI（1.0）** 等，可减少 Agent 自作主张引入 Supabase/OAuth 等。**1.0 上线后**仍需反复强调：**线上 mock 非 bug**，避免误报「假数据问题」。

---

## 4. 第九阶段走查与 9.2/9.3 工程实录（全文存放）

原 **§4～§7**（含 **9.3 问题表全文**、Sidebar 子清单、**P0～P3** 定义、9.2 Token 化复盘、Figma 协作教训、**稿差 / 占位**清单与处理建议）已 **整体迁至**：

**`docs/阶段复盘/Read2Action_1.0_第九阶段走查与问题闭环实录.md`**

> **`07` 本文件**自此 **不重复**粘贴上述长表；需要叙事、截图对照或面试素材时 **直接打开实录**。

---

## 5. 作品集与对外叙事建议

### 5.1 三条故事线（可任选组合）

1. **产品**：知识整理 → 行动转化 → 项目沉淀（价值链与 6 页闭环）。  
2. **工程**：mock-first、分阶段、9.3 走查表与 P0–P3、Token 化视觉体系统一、Next + shadcn 可维护结构。  
3. **交付**：GitHub + Vercel、线上可点、**明确写清 1.0 边界**（mock 非 bug）体现 **产品成熟度和诚信**。

### 5.2 建议展示的「证据截图 / 链接」

- 线上主链路关键帧：`/`、`/parsing` 四步、`/result`、保存 Toast、`/projects/sha`、详情双 Tab。  
- **GitHub** 与 **Vercel** 地址（见 `01`/`02`）。  
- **9.3 问题表** 前后对比（可选）：Sidebar 新增项目、⋯ 菜单、编辑 Toast、Tab 线、四步解析。

### 5.3 诚实边界（加分项）

主动说明 **未做真实 AI、未做登录数据库**，并指出 **1.1 路线图** — 比夸大成「完整 AI 产品」更可信。

---

## 6. 版本路线决策的可复用模板（1.0 / 1.1 / 2.0）

当「当前不做」清单过长时，容易与 **下一阶段** 混淆。本项目采用：

- **1.0**：冻结范围 + **上线事实**（mock 可接受）。  
- **1.1**：**单一清晰增量** — 已冻结为 **「真实 AI 文本总结版」**（文本 → AI → 结构化总结 → 结果页真实展示；见 `04` 2026-05-16、`02` §8）；工程上可表现为 **`/api/analyze`** 等，**仍排除**登录 / 数据库 / 链接抓取等（见 `03`）。  
- **2.0**：商业化与数据层能力打包为 **远期**，避免 1.1 scope creep。

---

## 7. 文档协作清单（给未来的你）

1. **决策只记一份详细版**：用 `04` 追加条目，避免只在聊天里「口头通过」。  
2. **进度只记一份快照**：用 `02` 更新阶段勾选，接力长文可拆到 `06`。  
3. **规则与交互给 Agent**：用 `05`，并视需要同步 `.cursor/rules`。  
4. **走查与复盘给作品集**：**过程全文**见 **`docs/阶段复盘/`**（如 **`Read2Action_1.0_第九阶段走查与问题闭环实录.md`**）；`07` 只保留 **方法提纲**。

---

## 8. Read2Action 1.0 复盘：AI 辅助设计开发工作流（ChatGPT 已确认 · 归档）

> 本节为 **1.0 周期结束后** 与 **ChatGPT** 对齐的复盘结论摘要（**2026-05-11**），按 **方法论与作品集素材** 职责写入；**非**对话原文归档。详细决策与日期仍以 `04_重要决策日志.md`、里程碑以 `06_版本更新记录.md` 为准。

### 8.1 Read2Action 1.0 实际「AI 设计 + 开发」工作流（概括）

- **产品 / 设计侧**：以 **PRD、范围与信息架构** 为锚点，经 **功能确认** → **低保真结构** → **高保真视觉说明（含 Figma 终稿）**，形成 **可被工程执行的规格**。  
- **工程侧**：在 **明确边界与 mock 策略** 下，用 **Cursor** 分阶段落地 **Next.js + 组件化 + mock 主链路**，并通过 **走查（9.3）与构建检查（10.0）** 收口。  
- **交付侧**：**用户人工验收** 主流程与线上表现后，完成 **GitHub / Vercel** 闭环，再把 **经验与坑** 写回 **docs**（含本节）。

### 8.2 ChatGPT / Cursor / 用户 — 三方角色分工

| 角色 | 典型职责 | 在本项目中的价值与风险 |
|------|----------|-------------------------|
| **ChatGPT** | 产品澄清、结构化 PRD、流程与文案推敲、复盘框架、风险清单 | **强**于把思路 **写清楚、写全**；需避免与 **实现细节 / 仓库真值** 脱节，结论应回写到 **docs** 对齐。 |
| **Cursor** | 依 docs + Figma **写代码、改 UI、跑通构建** | **强**于吞吐实现；易 **自作主张** 扩 scope（接库、接 AI、改架构），必须用 **文档边界 + 小任务 + 验收** 约束。 |
| **用户（你）** | **拍板范围**、**人工验收**、**关键节点合并与部署**、**把共识写进 docs** | **不可替代**：对「能上线演示吗」「是不是 mock」「哪些算 bug」拥有 **最终定义权**。 |

### 8.3 叙事链、提效判断、暴露问题与方法表（不重复展开）

与 **1.0 一页式复盘**、**1.1 总复盘** 交叉重复的 **线性叙事链**、**「AI 是否提效」判断**、**暴露问题列表**、**可复用方法表** — 在 **`07`** **不再全文粘贴**，避免与阶段复盘「必须保留过程」职责冲突。请直接阅读：

- **`docs/阶段复盘/Read2Action_1.0_复盘总结.md`** — 1.0 快速回看（分工、提效、方法论简表、对 1.1 启发）。  
- **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`** — **§8～§10**（设计师 × ChatGPT × Cursor 工作流、防自嗨、方法模板）。

### 8.4 一页式阶段总结（索引）

- 若需 **快速回看 1.0 全阶段叙事**（定位、交付、决策、问题、工作流、分工、提效、方法论、对 1.1 启发），见 **归档文件**（**非** `01`～`07` 日常维护主文档）：  
  - **`docs/阶段复盘/Read2Action_1.0_复盘总结.md`**

---

**文档路径**：`docs/核心记录/07_方法论沉淀.md`
