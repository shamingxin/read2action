# Read2Action · 03 下一步任务清单

> **用途**：在 **1.0 已冻结上线**、**1.1 已完成并冻结** 的前提下，列出 **1.2 启动准备与当前小步任务**、可选体验细修、并行排期，以及从零启动 Cursor 的历史参考。
> **合并来源**：原 `Read2Action_Cursor_新对话接力文档` §7–§8、`Read2Action_Cursor开发交接总文档` §16.4、`Read2Action_发给Cursor材料清单与操作步骤.md` 中分阶段指令与注意事项。

---

## 0. 当前默认立场（必读）

- **Read2Action 1.0** 已正式上线并 **冻结**；第十阶段 **10.0～10.2** 已完成。  
- **默认阶段**：**1.2 ✅ 全量冻结**（功能冻结 commit **`53482b2`**）。**1.3 ✅ 核心交付冻结**（2026-06-11；**1.3-0～D-4 ✅**；Production 线上验收通过；功能冻结 commit **`9c08757`**；详 **`04` 2026-06-11 · 1.3-D-4**、**`阶段复盘/Read2Action_1.3_账号登录与数据库版总复盘.md`**）。**1.4-C「核心体验补齐」进行中**：**C-0A / C-0B 已提交并上线**；**C-1「新建项目功能」已提交、push、上线并验收通过**；**C-1B「项目页与保存状态体验修正」已提交、push、上线并验收通过**；**C-1C「卡顿 / 交互反馈问题专项」已完成、push 并本地验收通过**（commits **`c2790d8`** / **`194a67c`** / **`6a1e81f`**）；**C-1D「全站文案 / 状态 / 术语一致性收口」代码部分已完成并 push**（commits **`9ef675a`** / **`6511879`** / **`deb6ba9b`** / **`95d7d41`** / **`452207c`** / **`806bc27`** / **`3b93112`**）。当前任务是 **C-1D docs-only 收口**；完成后进入 **1.4-C-2「项目菜单功能补齐」**，优先做项目重命名 / 删除。性能 / 卡顿优化暂缓并后置。  
- **功能代码纪律**：1.2 功能已冻结；**不再修改** `app/`、`components/`、`lib/` 等功能代码。  
- **Read2Action 1.1** 定义与交付已完成并冻结；当前以 **1.2 PRD + 启动方案** 为执行依据，不回滚到 1.1 开发态。  
- **业务代码纪律**：继续 **分小步、先列文件清单、经用户确认后开工**。解析链路仍执行 **A+B+C** 防重复请求机制；若触达解析链，仍建议 `npm run test` + Network 复验。  
- **不要**在未确认的小步范围内擅自接 **生产 AI**（指绕过已定契约乱接密钥/乱改页面）、上 **真实数据库**、做 **登录注册**、做 **链接抓取与全文解析管线**。  
- **不要**破坏 **1.0 冻结** 结论：不大改视觉与信息架构。  
- **不要**无边界扩张 **2.0** 功能。  
- **docs 整理**：`01`～`07` 只保留接力真值与索引；长过程、走查表、P0 数字等进 **`阶段复盘/`**；版本目标与验收进 **`版本启动方案/`** — 见 **`docs/README.md`**「**docs 整理原则**」。

以下清单中标注 **「需立项后」** 的项，在立项前仅作方案与文档对齐，不执行实现。

---

## 0.0-current 1.4-C-1D docs-only 收口（当前执行）

**状态**：1.4-C「核心体验补齐」进行中；**1.4-C-1 / C-1B 均已完成、上线并验收通过**；**1.4-C-1C「卡顿 / 交互反馈问题专项」已完成、push 并本地验收通过**；**1.4-C-1D「全站文案 / 状态 / 术语一致性收口」代码部分已完成并 push**。

**C-1D 完成记录**

1. **C-1D-1** 去掉左侧搜索入口：**`9ef675a fix: remove sidebar search entry`**。  
2. **C-1D-2** 去掉项目分享入口：**`6511879 fix: remove project share entry`**。  
3. **C-1D-3** 首页四张卡片文案调整：**`deb6ba9b fix: refine home feature card copy`**。  
4. **C-1D-4** 核心术语统一：**`95d7d41 fix: align core product terminology`**。  
5. **C-1D-5** 英文残留 / fallback 文案第二批：**`452207c fix: clean up remaining visible terminology`**。  
6. **C-1D-6** 空状态 / loading / error / fallback 文案统一：**`806bc27 fix: unify state copy`**。  
7. **C-1D-8** 分享残留与省略号文案小修：**`3b93112 fix: remove link copy remnants`**。  

**后续遗留**

1. 项目切换点击后仍可能需要 **1～2 秒** 才完成页面内容切换。  
2. 性能 / 卡顿优化暂缓并后置；当前不深入优化动态路由、数据加载、服务器区域、缓存或全局 loading。  

**当前下一步**

1. 完成本轮 **C-1D docs-only** 收口，记录 **C-1D 代码部分已完成并 push**。  
2. 完成后进入 **1.4-C-2「项目菜单功能补齐」**，优先做 **项目重命名 / 删除**。  
3. **笔记重命名 / 删除** 放到 **1.4-C-3**。  
4. **搜索 / 分享** 不在当前后续小步中实现。  

**当前边界（本轮不做）**

- 不改数据库结构。  
- 不改 Supabase 配置。  
- 不改 `/api/analyze`。  
- 不做搜索功能。  
- 不做分享功能。  
- 不做项目重命名 / 删除（放到 **1.4-C-2**）。  
- 不做笔记重命名 / 删除（放到 **1.4-C-3**）。  
- 不新增全站 `loading.tsx`。  
- 不处理 Next `middleware` → `proxy` 提示。  
- 不运行 `npm audit fix`。  
- 不升级 npm。  
- 不展示 `.env.local` 内容。  
- 不 `git add` / `commit` / `push`。  

---

---

## 0.0-archive 1.2 复盘文档沉淀（✅ 已完成 · docs-only）

> **前置**：1.2 功能已冻结（commit **`53482b2`**）。

**结果**：✅ 总复盘、核心记录、SOP、1.3/1.4 规划方向均已写入 docs；**docs commit / push 后 1.2 全量冻结**。

---

## 0.1 1.3「账号登录 + 数据库版」（✅ 核心交付冻结 · 2026-06-11）

> **前置**：1.2 功能 + 文档全量冻结。详 **`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md` §9.1**。

**定位**

让 Read2Action 从 **localStorage 本地工具**，升级为 **真正可持续使用的账号 + 云端数据库工具**。1.3 完成后，产品基础骨架基本完整，可以开始真实长期使用。

**核心目标（计划包含）**

1. 登录 / 注册  
2. 用户账号体系  
3. 项目数据入库  
4. 笔记数据入库  
5. 解析结果云端保存  
6. 换设备后可以看到自己的内容  
7. 为后续长期使用、作品集展示、真实产品化打基础  

**前置：设计规范基线（1.3-0）✅ 已冻结**

- 1.3 **不做大 UI 重构**。  
- **`docs/核心记录/Read2Action_界面设计规范_v1.0.md`** + HTML 预览已沉淀；视觉方向 **Notion 式 paper-calm productivity**。  
- **图标 / 插画 / 真实页面视觉统一** 后续再做（不阻塞 1.3-B～E）。  
- 后续 Cursor 做 UI 修改时，**须先读取该设计规范**，再按规范局部调整；**不能替代人工验收**。

**建议小步拆分（立项后执行）**

| 小步 | 内容 | 状态 |
|------|------|------|
| **1.3-0** | 设计规范 md 沉淀 + HTML 预览 | ✅ 已冻结 |
| **1.3-A** | Supabase 账号与数据库方案规划 | ✅ 已冻结（2026-06-09） |
| **1.3-B** | Supabase 项目创建与环境变量 | **✅ 已完成**（2026-06-10） |
| **1.3-C-1** | Supabase Auth 基础设施（`@supabase/ssr`、client、middleware、`/auth/callback`） | ✅ 本地验收（2026-06-10） |
| **1.3-C-2** | 登录 / 注册 / 手动登录 / 登出 / 侧栏登录态 | **✅ 事实冻结**（2026-06-10；见已知限制） |
| **1.3-D** | 云端项目 / 笔记保存 + 线上部署 | **✅ 完成**（**D-1～D-4 ✅**；Production 验收通过） |
| **1.3-E** | localStorage 迁移 | **待立项**（下一默认选项之一） |
| **1.3-F** | 轻量 UI 统一 + 体验优化 | **待立项**（含加载卡顿等非阻塞项） |

**1.3 交付收尾（✅ 已完成 · 2026-06-11）**

1. ✅ **1.3-0** 设计规范已冻结。  
2. ✅ **1.3-A** 方案已冻结 — **`docs/PRD/1.3/1.3-A_Supabase账号与数据库方案.md`**。  
3. ✅ **1.3-B 已完成**（2026-06-10）— Supabase 项目 `read2action`（`ap-southeast-1`，Healthy）；`.env.local` 已配置；Auth URL 已配置。详 **`09` §8.4**。  
4. ✅ **1.3-C** 事实冻结（2026-06-10）— Auth 基础设施 + 登录 / 注册 / 登出 / 侧栏登录态；详 **`04` 2026-06-10 · 1.3-C-2 事实冻结**。  
5. ✅ **1.3-D-1**（2026-06-11）— 数据层、`mappers`、`session`、`projects`、`notes` helper；**test 52 / build 通过**。  
6. ✅ **1.3-D-2**（2026-06-11）— 结果页「保存到项目」Supabase 写入；保存后停留 **`/result`**。  
7. ✅ **1.3-D-3**（2026-06-11）— 侧栏 / 项目详情读云端；退出登录跳 **`/`**；未登录回退保留。  
8. ✅ **1.3-D-4**（2026-06-11）— 云端 note 详情页修复（**`9c08757`**）；Vercel Production 部署；线上全链路验收通过。详 **`04` 2026-06-11 · 1.3-D-4**、**`阶段复盘/Read2Action_1.3_账号登录与数据库版总复盘.md`**。  
9. **待决策**：是否立项 **1.3-E**（localStorage 迁移）或 **1.3-F**（UI / 体验优化），或进入 **1.4** 规划。

**范围外**

- 登录 / 数据库实现代码；大 UI 重构；多模型；SEO；文件上传（属 1.4）。

---

## 0.2 1.4「文件上传 / 更多输入来源版」（规划备忘 · 非当前执行）

> **详 **`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md` §9.2**。

**定位**：从「粘贴文本解析」升级到「**文件内容解析**」。

**可能包括**：上传 PDF / Word / Markdown / TXT → 提取正文 → AI 结构化总结 → 保存到项目；可能支持更长文本。

**范围纪律**：**不建议提前过度扩大** — 涉及文件大小、格式解析、失败状态、隐私提示、存储策略等。

**移动端（Web H5 基础响应式）**

- **不做**独立 App / 小程序；**桌面端优先**，移动端基础可用。  
- 1.4 收尾阶段验收：首页输入、解析中、结果页、项目列表、笔记详情、侧栏 / 按钮不溢出、无横向滚动。

**1.4 完成后**：Read2Action Web 端基础功能 **阶段性收工**。**1.5 / 2.0** 暂不展开。

---

## 0.3 防自嗨检查（版本策略）

> Read2Action 当前阶段的目标不是无限扩展成大型知识库、Notion、飞书或 Agent 平台，而是做成一个 **真实能用、能展示、能复盘、能放进作品集的完整 Web 工具**。**1.3 打基础，1.4 补输入能力，之后阶段性收工**，是当前更健康的版本策略。

---

## 0.1-archive 1.2 上线准备（✅ 已完成 · 2026-06-09）

**目标**

1. 将 1.2 功能 commit 链 push 至 GitHub（`main`）。  
2. Vercel **Production** 重部署。  
3. 线上按 1.2 验收表做 E2E 抽检：空状态、全局自动暂存、项目归档、保存跳转、原文对照、解析单次 POST、宽屏阅读宽度。  
4. 抽检通过后 **1.2 阶段冻结**（功能冻结 commit **`53482b2`**）。

**结果**：✅ 全部完成；1.2 复盘 docs 见 **`docs/阶段复盘/Read2Action_1.2_复盘总结.md`**。

**范围外**

- 1.3 登录 / 数据库实现；最近列表状态标签；整体 UI 重构。

**验收标准**

1. 线上与本地主流程一致；`npm run test` / `lint` / `build` 通过。  
2. **1.2 总复盘**与核心 docs 已更新（见 **`阶段复盘/Read2Action_1.2_…总复盘.md`**）。

---

## 0.2 1.2 已完成小步归档（只读参考）

### 1.2-A：真实空状态 + mock 不默认展示 ✅ 已完成（2026-06-08）

**目标**

1. 将最近列表与项目相关列表统一到真实空状态体验。  
2. 彻底取消 mock 假数据的默认展示路径，避免真实状态被污染。  
3. 为后续 1.2 自动暂存/归档能力打下干净状态基线。

**范围内**

1. 最近列表在无本地记录时展示空状态引导。  
2. 项目内笔记列表在无本地记录时展示空状态引导。  
3. 默认关闭 mock 注入；仅允许开发开关临时启用 mock 视图（不进入默认产品路径）。  
4. 修复刷新后 mock 混入真实记录的可见问题（限 1.2-A 口径）。

**范围外（本阶段明确不做）**

1. 不做自动暂存（全局解析后自动写入 temporary）。  
2. 不做项目内自动归档（解析后自动写入项目）。  
3. 不接数据库。  
4. 不做登录注册。  
5. 不做文件上传。  
6. 不做链接抓取。  
7. 不做多模型 Provider 架构。

**验收标准（1.2-A）**

1. 清空 localStorage 后进入应用，最近列表与项目内笔记列表均显示真实空状态。  
2. 默认路径下不出现 mock 假数据；刷新后不发生 mock 回流污染。  
3. 不破坏 `/parsing` 防重复请求 A+B+C，不引入 `/api/analyze` 请求行为回归。  
4. 现有 1.1 已冻结链路可正常演示与访问。

---

### 1.2-B：记录生命周期补全 · 全局自动暂存 ✅ 已完成（2026-06-08 · 浏览器验收通过）

**目标**

1. 全局解析（首页等）成功后，结果自动写入 `localStorage` 为 **未归档** 记录。  
2. 用户不经「保存到项目」亦可在侧栏「最近」找回；点「保存到项目」后将 **temporary → saved** 并写入目标 `projectId`。  
3. 模型选择 UI 仅保留 **DeepSeek**（与本地 `AI_MODEL` 对齐；密钥仅服务端）。

**范围内**

1. `savedStatus: "temporary"` + `sourceContext: "global"` + 占位 `projectId`。  
2. `/parsing` 成功路径调用自动暂存（**不**新增 `/api/analyze` 请求）。  
3. `/result` 展示「已自动暂存，可保存到项目归档」。  
4. 「保存到项目」升级同一条 temporary 记录。  
5. 1.1 历史记录无 `savedStatus` 时默认视为 **saved**。  
6. `seedPendingAnalyzeSession` 抽取与单测；`_temporary` 路由供侧栏详情链路。

**范围外（本阶段明确不做）**

1. **项目内解析自动归档**。  
2. 最近列表 **「未归档 / 已保存」状态标签**。  
3. 最近列表 **直接归档**操作。  
4. Toast 文案精修、按钮 loading/disabled 全量优化。  
5. 数据库、登录、文件上传、链接抓取、多模型 Provider。  
6. **不改** `/api/analyze`；**不破坏** `/parsing` **A+B+C**。

**验收标准（1.2-B）· ✅ 已通过**

1. 首页模型只剩 DeepSeek；解析成功。  
2. `/result` 出现自动暂存提示。  
3. 不点保存时侧栏「最近」可找回；可进详情。  
4. 「保存到项目」后记录进入目标项目，Toast「已保存到项目」。  
5. test（32）/ lint / build 通过；`/api/analyze` 未改；单次 POST 机制未发现破坏。

**实录**：`docs/阶段复盘/Read2Action_1.2-B_全局自动暂存验收记录.md`

---

### 1.2-C / D / E 与总回归 ✅ 已完成（2026-06-08～09）

| 小步 | commit | 摘要 |
|------|--------|------|
| **1.2-C** | `ccdcc99` | 项目内解析自动 **saved + projectId** 归档 |
| **1.2-D** | `d56e39c` | 未归档详情「保存到项目」修复；**状态标签暂停** |
| **1.2-E** | `976c44b` | 原文对照 `rawContent`；隐藏未归档面包屑；解析真实已用时间 |
| **总回归 P2** | `d492e82` | 宽屏 `r2aContentPageShell`（`max-w-[960px]`） |

**总复盘**：`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md`

---

## 本轮 1.1 进度与下一小步（与 `02` §1.6、§8.2～§8.5 对齐）

- **`docs/版本启动方案/08_1.1真实AI文本总结启动方案.md`**：✅ **已新增并完成**（含文首 **`§0 执行摘要 / 最终执行口径`**；正文与 **`02`/`03`/`04`** 边界一致）。  
- **`docs/核心记录/01_项目总说明.md`**：✅ 文档索引已 **补充 `08` 引用**。  
- **1.1-B（开发任务拆解）**：✅ **已完成**（文件清单、阶段、风险）。  
- **第一小阶段**：✅ **已完成并已入库** — **仅**服务端 **`POST /api/analyze`**（Route Handler + `src/lib/analyze/*`、类型、`.env.example`、README、`.gitignore`）；**未改**首页、`/parsing`、`/result`、保存到项目、项目列表与详情。  
- **第二小阶段**：✅ **已接线并已入库**（经确认的最小改 **`home-note-panel.tsx`** / **`parsing-page-view.tsx`** / **`result-page-view.tsx`** + **`src/lib/analyze-client.ts`** 等）；**`/parsing` 调 `POST /api/analyze`**、**session 暂存**、**`/result` 优先读真实结果**；错误态按 **`08` §10**；**P0 重复请求已修复**（见 **`06` 2026-05-19**、**`07` §2.4**）；**Network 人工复验 ✅**（**`next dev --webpack`**；见 **`06` / `04` 2026-05-20**）。  
- **当前状态**：**第二、三小阶段均已落地**；**第三小阶段已正式冻结**（**`04` 2026-05-13** 文首 **「正式完成并冻结」**）。**GitHub / Vercel 线上验收 ✅**（**`04` 2026-05-13**、**`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**）。**1.1 正式总复盘**见 **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`**；**完全阶段冻结**待审阅确认（当前：**功能与线上验收 ✅**）。后续若再动解析链，**仍建议** **`npm run test`** + **Network**（单次 **`POST /api/analyze`**、未点重试）再短时真 API 抽检。  
- **开工前强制约定（后续小步继续适用）**：Cursor **必须先列出本步计划新增/修改的文件清单**，**经用户确认后**，**再开始写代码**。

### 1.1 总收尾（上线与线上验收 · **总复盘已形成**）

- **已完成**：本地三小步闭环；**`624428c`** push；Vercel **`AI_*`** 与 **Production** 部署；**read2action.vercel.app** 线上 **18** 项验收（详 **`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**、**`04` 2026-05-13**）。  
- **阶段口径**：**功能与上线验收完成，等待最终复盘与阶段冻结**（**未**进入 **1.2**）。  
- **待办顺序**：**提交** docs 收尾 → **1.1 总复盘**（含 **P0** 专题 **`07` §2.6**）→ **复盘通过后冻结 1.1** → **再** **1.2** 小 PRD。  
- **1.2 方向备忘**（**不实现**）：**「解析来源与自动暂存 / 自动归档机制」**（见下文 **1.2** 备忘节、**`04`**）。

### 第三小阶段 ✅ **已完成并正式冻结**（**本地 `localStorage` + `/result` 手动保存**）

- **目标**：用户在 **`/result`** 点「**保存到项目**」后写入 **`localStorage`**；侧栏「**最近**」与**项目页笔记列表**可见；进**详情**可回看；**刷新不丢失**；与 **mock 合并**、不破坏原 mock。  
- **边界（已遵守）**：未接 **数据库 / 登录 / 链接抓取**；未扩 **2.0**；**未**推翻 **A+B+C**；**未**破坏 **`/parsing` → `/api/analyze` → `/result`**；**未**做解析完成自动保存。  
- **冻结与验收**：**最终人工验收 ✅**、**正式完成并冻结** — 见 **`04` 2026-05-13** 文首 **「正式完成并冻结」**（含 **11** 条结论、侧栏「最近」刷新瞬间加载态备忘、**1.1** 边界重申）。  
- **交叉引用**：**`02` §8.4**；**`04` / `06` 2026-05-13**；交付细节见下节「**1.1 第三小阶段 · 交付收尾**」。

### 1.1 第三小阶段 · 交付收尾（**实现总结 / 变更文件 / 测试 / 人工验收**）

#### 实现总结（一句话）

在 **不接库** 前提下，把 **「当前 session 内成功解析结果」** 经 **手动保存** 写入 **`localStorage`**，与既有 **mock 笔记** 在 **侧栏「最近」、项目列表、详情页** 合并展示；**纯本地 `noteId`** 不再被服务端 **`notFound()`** 误杀。

#### 新增文件

| 文件 | 职责 |
|------|------|
| `src/lib/local-saved-notes.ts` | Key **`r2a:localSavedNotes`**；**`{ v: 1, notes }`**；安全读写、**同 `id` upsert**、按项目查找、**`r2a:local-saved-notes-changed`** 自定义事件 |
| `src/lib/local-saved-notes.test.ts` | 单测（含 **非法 JSON**、**setItem 失败**、**同 id 覆盖** 等） |
| `src/lib/note-from-last-analyze.ts` | **`ParseResultPreview` → `Note`**（**`user_note`**、**`rawContent` 兜底**、为 **`actionItems` / `knowledgeCards` 补 `id`** 等） |
| `src/lib/note-from-last-analyze.test.ts` | 映射与缺省字段单测 |
| `src/components/notes/note-detail-resolver.tsx` | **mock 优先**；否则 **客户端** 读 **localStorage**；均未命中 **`notFound()`** |

#### 修改文件

| 文件 | 变更要点 |
|------|----------|
| `src/components/result/save-to-project-dialog.tsx` | 读 session → 建 **`Note`** → **`appendOrUpsert`** → **派发事件** → **Toast** → **`/projects/{projectId}/notes/{newNoteId}`** |
| `src/components/layout/app-sidebar.tsx` | 「最近」**mock ∪ local**，**`updatedAt` 降序**取 **4** 条；监听 **自定义事件** + **`storage`** |
| `src/components/projects/project-page-view.tsx` | 项目内列表 **mock ∪ local**（同 **`projectId`**），**`updatedAt` 降序** |
| `src/app/projects/[projectId]/notes/[id]/page.tsx` | 非法 **`projectId`** 仍 **`notFound()`**；mock 未命中时挂载 **`NoteDetailResolver`** |

#### 工程测试结果（收尾时）

- **`npm run test`**：**4** 个测试文件，**26** 条用例 **全部通过**（其中 **`src/lib/analyze-client.test.ts`**、**`src/components/parsing/parsing-page-view.test.tsx`** 为 **第二小阶段**防回归；**`src/lib/local-saved-notes.test.ts`**、**`src/lib/note-from-last-analyze.test.ts`** 为 **第三小阶段**）。  
- **`npm run lint`**：**通过**。  
- **`npm run build`**：**通过**。（Vitest **`environmentMatchGlobs` deprecated** 提示可暂忽略。）

#### 人工验收 Checklist（第三小阶段）

1. **首页**：粘贴一段待解析 **纯文本** → 点「**开始解析**」→ 进入 **`/parsing`**。  
2. **`/parsing`**：Chrome DevTools **Network** 过滤 **`analyze`**；主路径 **未点「重试」** 时 **仅 1 条** **`POST /api/analyze`**、**Status 200**。（**本地 dev** 建议 **`npm run dev`** = **`next dev --webpack`**，见 **`02` §1.6**、**`06` 2026-05-20**。）  
3. 进入 **`/result`**：展示 **真实 AI** 结构化结果（与第二小阶段、**`08` §10** 一致；**非**成功路径静默 mock）。  
4. **`/result`** 点「**保存到项目**」→ 选项目 → 点弹窗底部「**保存**」：成功 **Toast「已保存到项目」**，跳转 **`/projects/{projectId}/notes/{新 noteId}`**（`noteId` 为浏览器生成的 UUID 或等价随机串）。  
5. **详情页**：与刚保存内容一致（**`rawContent`** 为兜底拼接，**非**完整粘贴原文，属本阶段预期）。  
6. 侧栏「**最近**」出现该条（与 mock **并集**、**`updatedAt` 降序**、最多 **4** 条）。  
7. 对应 **项目页** **`/projects/{projectId}`** 列表出现该条（**mock ∪ local**、排序一致）。  
8. **刷新详情页**（F5 / Cmd+R）：同一 URL 下记录 **不丢失**（未清理站点数据、**`localStorage`** 可用）。  
9. **刷新项目页**：列表仍含该条，**不丢失**。  
10. 原有 **mock** 笔记详情链接仍可打开（**`NoteDetailResolver`**：**mock 命中优先**）。  
11. 在本机再跑 **`npm run test`**、**`npm run lint`**、**`npm run build`**，均 **通过**。

#### 正式完成并冻结（最终人工验收）

- **状态**：上列 **Checklist** **✅ 最终人工验收全部通过**；本小阶段在 **产品 / 工程口径上正式冻结**（真值与 **11** 条结论、侧栏「最近」刷新瞬间 **hydration** 备忘、**1.1** 边界重申见 **`04` 2026-05-13** 文首 **「正式完成并冻结」**）。  
- **补充（「最近」刷新）**：整页刷新瞬间 **`localStorage`** 需在 **客户端 ready** 后再与 **mock** 合并，侧栏「最近」可能 **短暂**显示 **mock / 默认最近** — **不阻断** **1.1** 本小阶段收尾；**1.2** 或体验专项再统一处理。  
- **边界重申**：**1.1** **不做** 自动暂存/归档；**不接** 数据库/登录；**不做** 多模型 Provider 架构；**不改** **`/parsing` A+B+C**；**`1.2`** **「解析来源与自动暂存 / 自动归档」** 另立项（**`03`** 备忘、**`04`**）。

---

### 后续规划备忘：统一 AI Provider 架构（**1.2+ / 架构优化 · 需另立项**）

> **本阶段不实现**：当前已稳定的 **`/parsing` → `POST /api/analyze` → `/result` → 手动保存`** 主链路（含 **A+B+C**）**不因本条改代码**；以下仅作文档备忘，供未来 **1.2 或更后** 单独立项时对齐。

- **动机**：后续可能接入 **多个模型**（如 DeepSeek、通义、豆包、OpenAI 等），希望 **不要每接一家就重写一套端到端流程**。  
- **目标形态（方向性）**：  
  - **前端**只提交 **`text` + `modelProvider` + `modelId`**（及既有契约允许的字段），**统一走** **`POST /api/analyze`**；  
  - **服务端**按 **`provider` 分发**到不同 **模型适配器**（adapter）；各供应商返回后 **统一 `normalize`** 为当前 **`ParseResultPreview` / `Note`** 所需结构；  
  - **`/result` 展示与保存到项目**等消费侧 **不感知**具体模型差异（仅消费统一结构）。  
- **立项时再定**：请求体/环境变量/密钥与配额、超时与错误码映射、与现有 **`src/lib/analyze/*`** 的演进或并存方式。  
- **交叉引用**：**`04` 2026-05-13**（同日备忘条目）。

### 后续规划备忘：**1.2 解析来源与自动暂存 / 自动归档机制**（**1.2 后续子阶段备忘 · 非 1.2-A**）

**产品决策（待办记录）**：**不**在 **1.1 第三小阶段** 与当前默认排期内实现 **自动暂存 / 自动归档**；**1.1** 以 **`/result` 手动保存到项目** 收尾。**本条及以下小节**仅作 **1.2+ 产品与工程备忘**，立项前 **不写代码**。

> **本阶段不实现**：**1.1 第三小阶段 ✅** 已按 **「`/result` 手动保存到项目」** 收尾（**localStorage + mock 合并**等已落地部分 **不因本条回改**）；以下仅作文档备忘，供 **1.2** 单独立项时设计。  
> **不要**在未立项前改动已稳定的 **`/parsing` → `POST /api/analyze` → `/result` → 手动保存`** 主链路（含 **A+B+C**）。

**背景**

- 当前逻辑：**必须**在 **`/result`** 点击「**保存到项目**」后，笔记才进入 **localStorage**、侧栏「**最近**」与项目笔记列表；链路 **稳定**。  
- **产品缺口**：若用户从 **首页 / 全局「新笔记」入口** 解析后 **忘记保存**，后续 **不易找回** 本次解析结果（仍依赖 session 等临时态，体验上不够显式）。

**后续建议：解析入口拆两类**

1. **全局新笔记入口**（侧栏「新笔记」或首页输入）  
   - 解析完成 → **`/result`**；默认视为 **「未保存 / 未归档解析结果」**。  
   - **点击「保存到项目」** 后，才成为 **项目内正式笔记**。  
   - 若 **未保存**：后续可进入 **「解析历史 / 暂存池 / 未归档」** 等入口查看（**具体命名 1.2 再定**）。

2. **项目内解析入口**（已进入某项目页，顶部快速解析条）  
   - 用户已处于 **`projectId`** 上下文中，可默认本次解析 **归属当前项目**。  
   - 解析成功后 **自动保存到该项目**；**不再**要求二次选项目。  
   - 保存后进入 **该项目下新笔记详情**，或在 **项目列表即时出现**（具体跳转 1.2 再定）。

**后续实现建议（方向性）**

- 为解析流程增加 **`sourceContext`**（例如 **`global` / `project`**）。  
- **`sourceContext = global`**：结果先作 **未保存展示**；用户 **手动保存** 后入库（或等价持久化）。  
- **`sourceContext = project`**：携带 **`projectId`**；成功后 **自动保存** 到该项目。  
- 增加 **`savedStatus`** 或 **draft / temporary** 等状态，区分 **「已保存正式笔记」** 与 **「未归档解析历史」**。  
- **「最近」列表展示规则** 需在 1.2 明确：**仅正式笔记**，还是 **同时展示未归档解析**（及排序、上限、过期策略等）。

**当前结论（文档口径）**

- **1.1 第三小阶段**：**✅ 已按** **手动保存到项目** 收尾，**不并入**本条机制；**✅ 正式完成并冻结**（见 **`04` 2026-05-13** 文首 **「正式完成并冻结」**）。  
- **1.2**：单独设计并实现 **「解析来源与自动暂存 / 自动归档机制」**。  
- **交叉引用**：**`04` 2026-05-13**（**备忘：1.2 解析来源…**；与 **「第三小阶段收尾交付」** 条目分列）。

### 第一小阶段验收（本步已勾选）

- [x] `POST /api/analyze` 存在且符合 **`08` §6** 的请求/成功/错误 JSON 形状（含 **`LINK_INPUT_NOT_SUPPORTED`** 等 **服务端** 链接校验）。  
- [x] **`npm run lint`、`npm run build`** 通过；**无密钥**入库；环境变量名在 **README / `.env.example`** 可查；**`.env.local` 被 Git 忽略**。  
- [x] **首页、`/parsing`、`/result`、保存弹窗、项目列表与详情** 的 **行为与 1.0 线上一致**（本步 **零页面改动**）。

### 第二小阶段任务边界（已落地代码；复验已通过）

- **本步已做（代码）**：在 **`/parsing`** 内调用 **`POST /api/analyze`**；处理 **`ok: true`**（session 暂存后跳转 **`/result`**）与 **`ok: false`**（可理解错误 + **仅手动「重试」** 再请求，**禁止**静默回退 mock 冒充成功，见 **`08` §10**）；**`postAnalyze` 默认 30s 超时**；**防重复请求**（见 **`07` §2.4**）。  
- **本步刻意未扩**：**不接数据库 / 登录 / OAuth / Supabase**；**不做链接抓取**；**不大改视觉**；**不默认进入**「保存到项目」等下一小步（须 **`08` / `03`** 单独立项）。  
- **人工复验（2026-05-20 收束）**：**`dev`** = **`next dev --webpack`** 下 **Network** 确认 **仅 1 条 `POST /api/analyze`、200、进 `/result`、真实 AI**；**无** Turbopack **FATAL**（见 **`06` / `04`**）。  
- **后续仍建议**：重大改动前后 **`npm run test`** + **Network**（同上口径）防回归；**禁止**未做基本验证即对供应商无监督压测。  
- **后续小步开工约定**：**必须先列出计划修改文件清单，经用户确认后，再开始写代码**。

---

## Read2Action 1.1 范围边界（真实 AI 文本总结版）

> 与 `docs/核心记录/02_当前进度快照.md` **§8**、`docs/核心记录/04_重要决策日志.md` **2026-05-16** 条目一致；立项与排期须遵守。

### 定义

- **Read2Action 1.1 = 真实 AI 文本总结版**：在既有 **六页主流程** 下，将核心解析结果从 **mock** 升级为 **真实大模型基于用户输入文本生成的结构化总结**，并在 **结果页** 展示 **真实输出**。

### 1.1 只做

1. 用户输入 **一段待解析文本**（**1.1 以纯文本为可靠输入口径**；链接形态见下条「轻提示」策略）。  
2. **调用 AI**（后端 API + 模型）生成 **可映射到现有 UI 的结构化总结**。  
3. **结果页展示真实 AI 结果**（替换 mock 的核心体验；保存到项目等链路在立项中明确是否仍走当前前端状态或最小扩展，但 **不借机引入数据库/账号**）。

### 1.1 暂不做

- **链接全文抓取**、自动抓取网页/文章正文、任意 URL 内容拉取。  
- **登录注册**、**多用户**、账号体系与权限。  
- **数据库** 与服务端持久化（项目/笔记/用户数据落库等）。  
- **文件上传** 及基于文件的解析。  
- **浏览器插件**、RSS/剪藏类站外自动采集。  
- **复杂知识库**（超出「单段文本 → 单次结构化总结」的架构）。  
- **方法论自动生成**（不在 1.1 内把「方法论沉淀」升级为 AI 自动生成能力）。

### 用户输入为链接时（1.1）

- **不实现**自动读取链接正文。  
- **仅做轻提示**，建议文案口径：**「当前版本暂不支持自动读取链接正文，请复制正文后粘贴。」**（实现形态：Toast / 输入区提示等，立项时定稿。）

### 边界目的

- **避免 1.1 范围失控**（抓取、账号、落库最易把 1.1 拖成小 2.0）。  
- **先把产品从 mock 演示变成真实可用**：优先跑通 **「文本进 → 真 AI 出 → 结果页真展示」**。

---

## 1. 1.1 准备期 — 建议动作（文档 / 方案为主）

1. **接口形态**：对齐 **`/api/analyze`**（或等价路径）的请求/响应 JSON schema；**请求体以纯文本为主**；错误码与超时策略。  
2. **模型与密钥**：选型（单一供应商优先）、环境变量与 Vercel Secret、成本与限流。  
3. **UI 字段映射**：真实模型输出如何填入现有 **`ParseResultPreview`** / 结果页 / 详情页字段；缺失字段时的降级展示。  
4. **`/parsing` 四步与真实耗时**：状态机设计（假进度 vs 真实阶段事件）、最短展示时间、失败重试与文案。  
5. **链接形态与轻提示**：如何检测「主要为 URL 的输入」、提示文案与不打断主流程的策略（**不**在 1.1 内做抓取兜底）。  
6. **安全与合规**：用户输入日志是否存储、PII、内容审核策略（初版可「不落库」）。  
7. **验收标准**：单独立项文档中写清「1.1 Done」的 E2E 清单（含线上 staging），且 **验收范围不得超过** 上文「只做 / 暂不做」。

---

## 2. 已知细修项（不阻塞 1.0；可选 / 体验专项）

由原接力文档 **§7**、进度快照 **§7**、交接 **§16.4** 合并：

| 类别 | 项 | 备注 |
|------|-----|------|
| 视觉 | 字体与稿非像素级一致 | 迭代 token |
| 视觉 | 间距 / 圆角 / 阴影 | 已 token 化主要壳，可继续收束 |
| 一致性 | 日期格式列表 `en-US` vs 详情 `zh-CN` | 全站统一策略 |
| 布局 | 部分内部 `max-w` 与 1020 壳并存 | 按需收束 |
| Figma | 「05-笔记详情-内容总结」读稿 | 第六阶段曾 MCP 未连接，nodeId 未稳定记录 |
| 内容 | **方法论沉淀** 占位文案 | 详情页仍为占位 |
| 内容 | **`/result`「原文对照」Tab** | 仍为占位 |
| 交互 | **「取消解析」确认弹窗** | 早期交互文档有写；实现曾为直接回首页；若与稿一致可小步补 |
| 功能 | 真实导出 / 附件 / 删除 | 1.0 Toast 占位；属 1.1+ / 2.0 |

---

## 3. 并行排期（可选，非 1.1 首包阻塞）

- `/result` 原文对照真实内容（若依赖 **抓取或外部正文拉取**，则 **不属于** 1.1「文本总结版」默认可做范围，须 **另立项**；若仅展示 **用户粘贴的同一正文** 或与总结字段强绑定且不引入抓取，再在方案中评估）。  
- 方法论占位替换为 **人工撰写的产品文案**（**非**「方法论自动生成」；后者不在 1.1 范围）。  
- Figma「05」读稿与像素对齐。  
- 日期与栅格统一。  
- 取消解析确认弹窗。

---

## 4. 1.1 暂不要做（直至单独立项）

以下与 **§「Read2Action 1.1 范围边界」** 一致，强调 **防 scope 失控**：

- 未立项范围下：**生产 AI**、**真实数据库**、**登录注册**、**多用户**、**文件上传**、**浏览器插件**。  
- **链接全文抓取**、自动拉取 URL 正文、站外采集类能力。  
- **复杂知识库**、**方法论自动生成** 等超出 **「单段文本 → 结构化总结」** 的能力。  
- **真实删除 / 重命名 / 分享**（除非产品改边界并立项）。  
- **2.0 视觉大改**、无边界 **2.0** 功能扩张。

---

## 5. 发给 Cursor 的材料与目录（已更新为新文件名）

### 5.1 必发

1. Figma 设计稿链接  
2. `docs/核心记录/05_Cursor执行规则.md`（含 Rules 与提示词）  
3. `docs/核心记录/01_项目总说明.md`  
4. `docs/核心记录/02_当前进度快照.md`  
5. 交互细节：`05` 附录「交互与跳转逻辑」或全文在 `05` 内  

### 5.2 建议一起发

6. `docs/核心记录/04_重要决策日志.md`  
7. `docs/核心记录/03_下一步任务清单.md`（本文件）  
8. `docs/核心记录/06_版本更新记录.md`  
9. `docs/核心记录/07_方法论沉淀.md`  
10. **`docs/版本启动方案/08_1.1真实AI文本总结启动方案.md`**（**1.1 契约**与 **§0 执行摘要**；进入 1.1 代码前必读）

### 5.3 推荐目录结构（参考）

```text
read2action/
├── docs/
│   ├── README.md
│   ├── 核心记录/
│   │   ├── 01_项目总说明.md
│   │   ├── 02_当前进度快照.md
│   │   ├── 03_下一步任务清单.md
│   │   ├── 04_重要决策日志.md
│   │   ├── 05_Cursor执行规则.md
│   │   ├── 06_版本更新记录.md
│   │   ├── 07_方法论沉淀.md
│   │   └── Read2Action_协作流程SOP_v1.0.md
│   ├── 版本启动方案/
│   │   └── 08_1.1真实AI文本总结启动方案.md
│   └── 阶段复盘/
│       ├── Read2Action_1.0_复盘总结.md
│       ├── Read2Action_1.1_上线与验收收尾记录.md
│       └── Read2Action_1.1_真实AI文本总结版总复盘.md
├── .cursor/
│   └── rules/
│       └── （可选）从 05 中 Rules 节复制出的规则文件
└── …
```

---

## 6. 第一次打开 Cursor 后怎么做（操作步骤）

### Step 1：打开项目文件夹

用 Cursor 打开本地仓库根目录。

### Step 2：新建 Cursor 对话

可先发：

```text
请先阅读 docs/README.md（目录三类），再阅读 docs/核心记录/01_项目总说明.md、docs/核心记录/02_当前进度快照.md、docs/核心记录/04_重要决策日志.md、docs/核心记录/05_Cursor执行规则.md，理解 1.0 范围与当前进度。先不要写代码，读完后请总结开发目标、页面范围、路由与当前阶段边界。
```

### Step 3：发 Figma 链接

```text
这是 Read2Action 1.0 的 Figma 设计稿链接：
【粘贴你的 Figma 链接】

请以 Figma 为视觉标准；当前 1.0 已冻结上线，若未立项 1.1，请不要擅自改业务代码。
```

### Step 4：每次改代码时的沟通习惯

```text
请先说明你准备修改哪些文件，再开始修改。
修改后请总结：
1. 修改了哪些文件
2. 实现了什么功能
3. 如何测试
4. 是否有遗留问题
```

不要一次性让 Agent「把整个项目做完」；分阶段小步推进。

---

## 7. Cursor 开发分阶段指令（历史参考 — 1.0 已基本完成）

> 以下段落来自原《发给 Cursor 材料清单》，适用于 **从零重做教学** 或 **新成员模拟路径**；当前仓库 **1.0 已完成**，请勿在未立项下机械执行「继续开发」类指令。

### 第一阶段：项目初始化 + 基础结构

```text
请先完成第一阶段：
1. 初始化 Next.js + TypeScript + Tailwind CSS 项目
2. 配置 shadcn/ui 和 lucide-react
3. 创建基础目录结构
4. 创建 types/index.ts
5. 创建 data/projects.mock.ts、data/notes.mock.ts、data/result.mock.ts
6. 创建 AppLayout 和 AppSidebar
7. 先保证首页能显示左侧 Sidebar 和右侧空内容区

不要开发全部页面，完成后告诉我改了哪些文件。
```

### 第二阶段：首页

```text
请开发首页 / 新笔记输入页：
1. 还原 Figma 首页布局
2. 实现统一大输入框
3. 输入为空时开始解析按钮禁用或置灰并可 Toast
4. 输入有内容时按钮可点击
5. 点击开始解析跳转 /parsing
6. 模型下拉可以先做静态
7. 底部展示四个“你将获得”的卡片
```

### 第三阶段：解析中

```text
请开发 AI 解析中页面：
1. 路由为 /parsing
2. 展示标题与四步进度
3. 展示 LIVE PREVIEW 骨架屏
4. 四步依次展示完成后跳转 /result（时长以产品为准）
5. 点击取消解析返回首页并清除定时器，无幽灵跳转
```

> **注**：早期材料写「2–3 秒」「确认弹窗」；**9.3 终态**为四步完整展示 + 约 3.5～4.5s 量级、取消直接回首页 — 以 **`01` / `02`** 与代码为准。

### 第四阶段：解析结果

```text
请开发解析结果页：
1. 路由为 /result
2. 使用 result.mock.ts 数据
3. 展示一句话总结、核心观点、行动清单、知识卡片
4. 实现保存到项目弹窗，默认保存到“沙”
5. 保存成功后 Toast 提示“已保存到项目”
6. 可跳转 /projects/sha
7. 编辑和导出先用 Toast mock
```

### 第五阶段：项目目录

```text
请开发项目目录页：
1. 路由为 /projects/[projectId]
2. 展示当前项目名
3. 展示顶部快速解析输入条
4. 展示项目内笔记列表表格
5. 点击笔记行进入详情页
6. 搜索支持按标题和标签前端过滤
7. 更多操作和分页可以先静态或 mock
```

### 第六阶段：笔记详情

```text
请开发笔记详情页：
1. 路由为 /projects/[projectId]/notes/[id]
2. 展示面包屑、标题、来源、标签、创建时间、字数
3. 实现内容总结 / 原文对照 Tab 切换
4. 内容总结展示总结、知识卡片、核心观点、行动清单、原文摘录
5. 原文对照展示原始内容
6. checkbox 支持本地切换状态
```

### 第七阶段：统一视觉与交互

```text
请统一检查所有页面：
1. 是否接近 Figma
2. Sidebar 是否复用
3. 页面间距、圆角、边框、阴影是否统一
4. hover / focus / disabled 状态是否存在
5. 文案是否正确
6. 是否没有出现“保存到知识库”等旧文案
7. 主流程是否能完整跑通
```

---

## 8. 新 Cursor 对话开头提示词（已更新路径）

```text
请先阅读 docs/README.md，再阅读项目 docs/核心记录/02_当前进度快照.md（§1.6 接力节点、§8.2～§8.5）、docs/核心记录/03_下一步任务清单.md「本轮 1.1 进度与下一小步」、docs/核心记录/04_重要决策日志.md（含 2026-05-13 上线验收条）、docs/核心记录/05_Cursor执行规则.md、docs/核心记录/01_项目总说明.md，以及 docs/版本启动方案/08_1.1真实AI文本总结启动方案.md（优先 §0 执行摘要）；可选补充 docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md。理解 1.0 范围与 1.1 当前进度。

第一至第八阶段已完成；第九阶段 9.1、9.2、9.3 均已通过；第十阶段 10.0、10.1、10.2 均已完成。Read2Action 1.0 已正式上线并冻结。线上：https://read2action.vercel.app ；仓库：https://github.com/shamingxin/read2action 。

1.1 范围已冻结为「真实 AI 文本总结版」。第一小阶段（仅服务端 POST /api/analyze）已入库。第二小阶段已接线：首页 sessionStorage 待解析正文 → /parsing 单次 POST /api/analyze（30s 超时、防重复请求 A+B+C）→ /result 读 session 展示真实结果；P0 全过程与 webpack dev 复验见 02 §1.6、07 §2.4～§2.6、06/04 的 2026-05-20 与 2026-05-21。第三小阶段（localStorage 手动保存 + mock 合并）✅ 已完成并正式冻结，见 04 2026-05-13 文首「正式完成并冻结」、03「1.1 第三小阶段 · 交付收尾」、02 §8.4。GitHub 624428c 与 Vercel Production 线上真实 AI + 本地保存闭环已验收，见 04 2026-05-13、docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md、02 §8.5；**1.1 正式总复盘**见 **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`**；**完全阶段冻结**待审阅确认。**1.2 已进入启动准备**（PRD 完成 + 启动方案创建），当前默认执行 **1.2-A：真实空状态 + mock 不默认展示**；自动暂存/归档与统一多模型 Provider 属后续子阶段，不在当前小步实现。不要超范围改页面；不要接数据库 / 登录 / 链接抓取；不要无边界扩展 2.0 功能。
```

---

**文档路径**：`docs/核心记录/03_下一步任务清单.md`
