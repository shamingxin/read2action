# Read2Action · 04 重要决策日志

> **用途**：记录关键阶段验收、范围边界与文档同步节点，供新对话快速对齐「为什么当时这样定」。  
> **交叉引用**：`docs/核心记录/02_当前进度快照.md`、`docs/核心记录/03_下一步任务清单.md`、`docs/核心记录/06_版本更新记录.md`。

---

## 2026-06-10 · 1.3-B 人工操作完成 · 进入 1.3-C 前提条件已就绪

**背景**

- 1.3-B 启动方案已立项（SQL 草案 + env 清单）；开发者按启动方案 §8.3 完成全部人工操作。

**结论**

- **Supabase 项目已创建**：Project name `read2action`，Region `ap-southeast-1`（Southeast Asia, Singapore），Status Healthy，GitHub 未连接。
- **本地 `.env.local` 已配置**：包含 `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`（publishable key）、`AI_API_KEY`（DeepSeek）；**docs 不记录任何密钥原文**。
- **Supabase Auth URL Configuration 已设置**：Site URL `http://localhost:3000`；Redirect URL `http://localhost:3000/auth/callback`。
- **`.env.local` 安全确认**：未被 Git 跟踪；`git status --short` 无输出；`.gitignore` 包含 `.env*` 与 `!.env.example`。
- **安全边界**：service_role key、数据库密码、AI_API_KEY 仅用于服务端 / 本地；绝不写入前端代码、docs、截图或 Cursor 对话。**当前阶段只保存 publishable/anon key，service_role key 暂不复制、不使用。**
- **阶段结论**：1.3-B 完成 Supabase 项目创建与环境变量准备；**未执行 SQL、未改业务代码、未安装依赖、未接入 Auth**。
- **下一步**：立项 **1.3-C**（Supabase Auth 接入开发，`@supabase/ssr` + `/login` + Middleware）。

**同步**

- **`02` / `03` / `06`**。

---

## 2026-06-10 · 1.3-B Supabase 项目创建与环境变量启动方案立项

**背景**

- 1.3-A 已冻结（2026-06-09）；1.3-B 为第一个准备接触真实 Supabase 的阶段，须先完成文档立项，再由开发者手动建项目、执行 SQL。

**决策**

- **1.3-B ✅ 启动方案已立项**（2026-06-10）：**`docs/版本启动方案/09_1.3-B_Supabase项目创建与环境变量启动方案.md`**。
- **本阶段交付**：SQL migration 草案（三表 + Trigger + RLS）、`.env.example` 草案、Vercel 环境变量配置清单、Supabase Dashboard 建项目步骤。
- **本阶段不做**：不改业务代码、不安装依赖、不接 Supabase SDK、不改 `.env.local` 真实密钥、不写登录/注册 UI。
- **SQL 草案状态**：仅为文档草案，**未在 Supabase 执行**；1.3-C 开工前由开发者手动执行。
- **进入 1.3-C 的前提**：Supabase 项目已创建、三项 key 已获取、`.env.local` 已填写、SQL 已执行、Vercel env 已配置。
- **下一步**：开发者完成人工操作（见启动方案 §8.3）→ 立项 1.3-C（登录/注册最小闭环）。

**同步**

- **`02` / `03` / `06`**；SQL 草案见 **`09` §4～§5**。

---

## 2026-06-09 · 1.3-A Supabase 账号与数据库方案冻结

**背景**

- 1.3-0 设计规范已冻结；需将账号、最小三表 schema、RLS、localStorage 迁移、环境变量与分阶段计划固化为可执行方案，再进入 1.3-B。

**决策**

- **1.3-A ✅ 已冻结**（2026-06-09 人工确认）：**`docs/PRD/1.3/1.3-A_Supabase账号与数据库方案.md`**。  
- **技术栈**：Next.js + Supabase Auth + PostgreSQL + Vercel；AI 仍走现有 **`POST /api/analyze`**（A+B+C 不变）。  
- **账号**：游客 localStorage 全功能 + 登录云端模式；**不强制注册**。  
- **数据库（最小）**：**`profiles` / `projects` / `notes`**；**`analysis_results` / `local_migration_logs` 暂缓**；迁移态用 **`profiles.migration_status`**；**`notes.local_id`** 幂等防重复迁移。  
- **安全**：全表 RLS + `user_id = auth.uid()`；anon key 可前端；**service_role / AI_API_KEY 仅服务端**。  
- **下一步**：**1.3-B 启动方案立项** → Supabase 项目与环境变量（**本决策不进入开发**）。

**同步**

- **`02` / `03` / `06` / `docs/README.md`**。

---

## 2026-06-09 · 1.3-0 设计规范冻结 · 1.3-A Supabase 方案启动

**背景**

- 1.2 已全量冻结；1.3 启动前须先沉淀 UI 基线，再规划账号与数据库，避免 scope 与视觉双漂移。

**决策**

- **1.3-0 ✅ 已完成并冻结**：**`docs/核心记录/Read2Action_界面设计规范_v1.0.md`** + **`Read2Action_界面设计规范_v1.0_preview.html`**（人工确认）。  
- **视觉方向**：**Notion 式 paper-calm productivity**（暖纸感画布、白卡片、单一蓝色主操作色）；**非** Notion 品牌复刻。  
- **后续专项**：图标、插画、真实页面全站视觉统一 **不在 1.3-0 阻塞**；在 **1.3-F 或更后** 分步做。  
- **1.3-A 当前**：仅产出 **`docs/PRD/1.3/1.3-A_Supabase账号与数据库方案.md`**；技术栈 **Next.js + Supabase Auth + PostgreSQL + Vercel**；**不写代码、不装依赖、不接 Supabase**。  
- **下一步**：1.3-A 人工确认 → **1.3-B** Supabase 项目与环境变量。

**同步**

- **`02` / `03` / `06`**；设计规范路径见 **`Read2Action_界面设计规范_v1.0.md`**。

---

## 2026-06-09 · 1.3 / 1.4 后续规划方向固化 · 1.2 文档全量冻结

**背景**

- 1.2 功能已冻结（commit **`53482b2`**）；复盘 docs 已完成最终沉淀。
- 需明确 1.3 / 1.4 规划方向，避免 scope 漂移或过早扩展为大型知识库平台。

**决策**

**1.3「账号登录 + 数据库版」**

- 核心目标：localStorage → 账号 + 云端数据库；换设备可找回内容；1.3 完成后产品基础骨架基本完整。  
- 计划：登录/注册、账号体系、项目/笔记/解析结果入库、本地数据迁移。  
- **1.3-0 前置**：沉淀 **`docs/核心记录/Read2Action_界面设计规范_v1.0.md`**（立项时创建）；1.3 不做大 UI 重构；Cursor 改 UI 须先读规范。  
- 小步：1.3-0～1.3-F（见 **`03` §0.1**、**`1.2 总复盘` §9.1**）。  
- **当前只记录规划，不创建完整 1.3 PRD，不实现功能**。

**1.4「文件上传 / 更多输入来源版」**

- 核心目标：粘贴文本 → 文件内容解析（PDF/Word/Markdown/TXT 等）。  
- **不建议提前过度扩大**（文件大小、格式、隐私、存储策略）。  
- 移动端：**Web H5 基础响应式**；不做 App/小程序；桌面优先。  
- 1.4 完成后 Web 端基础功能 **阶段性收工**。**1.5 / 2.0 暂不展开**。

**防自嗨检查**

> Read2Action 目标是做成真实能用、能展示、能复盘、能放进作品集的完整 Web 工具。**1.3 打基础，1.4 补输入能力，之后阶段性收工**。

**1.2 全量冻结**

- 功能冻结 commit **`53482b2`**；复盘文档已完成沉淀。  
- **docs commit / push 后 1.2 正式全量冻结**；不再修改 1.2 功能代码。

**同步**

- **`1.2 总复盘` §9～§10**；**`03` §0.1～0.3**；**`02` / `06` / `07` / SOP**。

---

## 2026-06-09 · docs 四类结构规范固化 · 1.2 复盘文档沉淀

**背景**

- 1.2 功能已冻结（commit **`53482b2`**），进入复盘与 docs 收口阶段。
- 需避免后续版本新建 `retrospectives/`、`sop/` 等平行目录，造成 docs 结构混乱。

**决策**

- Read2Action 后续 docs **统一沿用现有四目录结构**：

```text
docs/
├── README.md
├── PRD/
├── 核心记录/
├── 版本启动方案/
└── 阶段复盘/
```

- **不新增**平行英文目录（`retrospectives/`、`sop/`、`progress/`、`decisions/` 等）。
- **不大规模重命名或搬迁**历史文件。
- 后续版本 **只按既有目录新增或更新文档**。
- **功能冻结后只允许 docs-only 更新**；版本收尾必须同步更新：复盘、进度快照、任务清单、决策日志、版本记录、SOP。

**同步**

- **`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md` §7** 写入正式结论。
- 更新 **`docs/README.md`**、**`02` / `03` / `06` / `07` / SOP §16～§17**。

---

## 2026-06-09 · Read2Action 1.2 已 push / 已部署 / 线上验收通过 / 正式冻结

**背景**

- 1.2 功能闭环通过后完成 push、Vercel 部署与线上 E2E 抽检。
- 需固化 **1.2 冻结版本**、复盘文档与 1.3 启动边界。

**决策**

- **1.2 正式冻结**（2026-06-09）；**功能冻结 commit**：**`53482b2`** docs: finalize 1.2 release notes。
- **线上验收**：通过（空状态、自动暂存、项目归档、保存跳转、原文对照、解析单次 POST、宽屏等）。
- **复盘 docs**：新增 **`docs/阶段复盘/Read2Action_1.2_复盘总结.md`**；SOP **`§16 AI 协作可复用工作流`** 自 1.2 复盘沉淀。
- **下一阶段**：**1.3 登录与数据库**须单独立项 PRD（**不在本步写代码**）。

**同步**

- 更新 **`02` / `03` / `06` / `docs/README.md`**；技术总复盘 **`Read2Action_1.2_…总复盘.md`** 标记冻结状态。

---

## 2026-06-09 · Read2Action 1.2 功能闭环通过 · 进入上线准备

**背景**

- 1.2-A～C 已分别验收并 commit；1.2-D（未归档详情保存修复）、1.2-E（原文对照 / 面包屑 / 解析计时）、总回归宽屏 P2（`d492e82`）均已完成。
- 需固化 **1.2 最终状态**、暂停项与下一阶段边界。

**结论**

- **1.2 功能闭环验收通过**（2026-06-09）；**P0 / P1 阻塞：无**；**test（36）/ lint / build** 通过。
- **最终 commit**：`d492e82 fix: constrain content width on wide screens`（含此前 `976c44b`、`d56e39c` 等 1.2 功能链）。
- **暂停 / 不纳入 1.2**：最近列表「未归档 / 已保存」**状态标签**；最近直接归档；Toast 精修等。
- **关键产品决策（本版固化）**：
  1. 未归档详情页 **隐藏面包屑**，避免 `/_temporary` 链接触发 404；
  2. `/parsing` 展示 **真实已用时间**，不做 AI 耗时预测；
  3. 宽屏下结果页 / 详情页 **`max-w-[960px]`** 收敛，不做整体 UI 重构。
- **下一阶段**：**1.2 上线准备**（push + Vercel + 线上抽检）→ **1.2 冻结** → **1.3 登录与数据库**须单独立项 PRD。

**文档动作**

- 新增 **`docs/阶段复盘/Read2Action_1.2_解析记录暂存与归档体验优化版总复盘.md`**。
- 同步 **`02`**、**`03`**、**`06`**、**`01`**、**`docs/README.md`**。

---

## 2026-06-09 · 1.2-D 最近列表状态标签暂停 · 未归档面包屑隐藏

**背景**

- PRD 1.2-D 曾规划最近列表状态标签；实现中发现与当前侧栏信息密度、验收优先级不匹配。
- 未归档详情页若展示面包屑并点击项目名，会进入 `/_temporary` 路由导致 **404**。

**结论**

- **最近列表状态标签：暂停，不纳入 1.2**（含 1.2 总收尾范围）。
- **未归档详情页不渲染面包屑**；已归档项目笔记面包屑正常，项目名可点击跳转。
- **1.2-D 实际交付**：未归档详情页「保存到项目」无反应修复（commit `d56e39c`）。

---

## 2026-06-09 · 1.2-E 原文对照与解析反馈 · 总回归宽屏 P2

**背景**

- 1.2-C 后原文对照仍为占位；`/parsing` 固定「预计 20 秒」与真实耗时脱节；总回归发现宽屏下内容区过宽。

**结论**

- **1.2-E**（`976c44b`）：贯通 `rawContent`；`/result` 与详情页展示真实粘贴原文；`/parsing` 改为 **已用 N 秒**（StrictMode 下计时 ref 防闪回）。
- **总回归 P2**（`d492e82`）：新增 `r2aContentPageShell`（`max-w-[960px]`），仅结果页 / 笔记详情页；小屏仍 `w-full` 响应。

---

## 2026-06-08 · 1.2-C「项目内解析自动归档」人工验收通过（功能 commit 已收口）

**背景**

- 1.2-C 在 1.2-B 全局自动暂存基线上，补齐 **项目页内解析入口** 的 **自动归档** 闭环：项目页发起解析成功后，结果直接写入当前项目，无需手动「保存到项目」。

**结论**

- **1.2-C 人工验收通过**（2026-06-08）。  
- **功能 commit**：`ccdcc99 feat(1.2-C): 项目内解析自动归档闭环`。  
- **验收结果**：
  1. 项目页内发起解析后成功进入 **`/parsing`** ✅  
  2. **`/parsing`** 成功后自动保存到当前项目 ✅  
  3. **`/result`** 显示绿色「**已保存到项目**」 ✅  
  4. 记录为 **`savedStatus: "saved"`**、**`sourceContext: "project"`**、正确 **`projectId`** ✅  
  5. 项目详情页可见新记录，**无需**手动保存 ✅  
  6. 首页解析回归正常，仍为 **temporary/global** ✅  
  7. **`/api/analyze`** 未修改 ✅  
  8. **`/parsing`** 单次请求机制未破坏 ✅  
  9. **test / lint / build** 均通过 ✅  
  10. **原文对照**是历史占位，**不属于** 1.2-C 回归 ✅  
- **明确未做**：最近状态标签、最近直接归档、Toast 精修、loading/disabled 全量优化；**不进入 1.2-D**。  
- **Git 纪律**：功能 commit 已收口；本轮 **docs 最小收尾** 待用户确认后 commit；**禁止** Cursor 自动 commit / push / 配置 remote。

**文档动作**

- 同步更新 **`02`**、**`06`**、**`04`**（本条目）。

---

## 2026-06-08 · 1.2-B「全局自动暂存」浏览器验收通过（代码待 commit）

**背景**

- 1.2-B 在 1.2-A 空状态基线上实现全局解析自动 **temporary** 暂存、`/result` 状态提示、「保存到项目」升级 **saved**、DeepSeek 单模型 UI。  
- 实现过程中曾单独处理模型选择收口（仅 DeepSeek），与自动暂存主链路一并纳入本小步验收。

**结论**

- **1.2-B 浏览器主流程验收通过**（2026-06-08）。  
- **验收结果**：首页仅 DeepSeek → 解析成功 → `/result` 自动暂存提示 → 侧栏「最近」可找回（未手动保存）→ 详情可进 → 「保存到项目」后进入目标项目并 Toast 成功。  
- **工程**：`npm run test`（32）、`npm run lint`、`npm run build` 通过。  
- **风险约束**：**未修改** `/api/analyze`；**未破坏** `/parsing` **A+B+C** 单次请求机制；API Key **未**进前端或文档。  
- **明确未做**：项目内自动归档、最近状态标签、最近直接归档、数据库/登录等（见 **`1.2-B 验收记录`**）。  
- **Git 纪律**：业务代码与 docs **尚未 commit**；**禁止** Cursor 自动 commit / push / 配置 remote。  
- **下一阶段**：**1.2-C 暂不开始**；进入前须 **单独拆小阶段** 并列文件清单经用户确认。

**文档动作**

- 新增 **`docs/阶段复盘/Read2Action_1.2-B_全局自动暂存验收记录.md`**。  
- 同步更新 **`02`**、**`03`**、**`06`**、**`docs/README.md`**。

---

## 2026-06-08 · 1.2-A「真实空状态 + mock 不默认展示」验收通过并本地提交

**背景**

- 1.2-A 于本轮正式启动并落地，完成了「默认首页不展示 mock、真实空状态可用、/projects/1 不再 404」等目标。
- 本轮过程中 Cursor 曾误创建本地 commit，已通过 `git reset --mixed 5b0a0b6` 恢复，代码改动保留，重新创建正式 commit。

**结论**

- **1.2-A 验收通过**，本地 commit `14fec6d fix(1.2-A): 修复空状态验收阻塞问题` 已创建。
- **验收结果**：
  - `/` 默认首页不展示 mock ✅
  - `/?mock=true` 展示 mock 最近列表 ✅
  - `/projects/1` 不再 404，展示项目空状态 ✅
  - `/projects/1?mock=true` 不再 404 ✅
  - 首页输入框上方空状态提示（红框）已移除 ✅
  - `npm run lint` 通过 ✅
  - `npm run build` 通过 ✅
- **变更文件**：`src/components/input/home-note-panel.tsx`、`src/data/projects.mock.ts`。
- **Cursor 执行纪律追记**：本轮 Cursor 曾在未经用户确认下自动创建本地 commit；已明确后续规则：**禁止自动 commit、禁止自动 push、禁止配置 remote**，所有 git 操作须经用户明确指令后执行。
- **下一阶段**：**1.2-B 已标记为下一步，暂不开始开发**。

**文档动作**

- 同步更新 `02_当前进度快照.md`、`03_下一步任务清单.md`、`06_版本更新记录.md`。业务代码已在本 commit 收口。

---

## 2026-06-05 · Read2Action 1.2 正式进入启动准备

**背景**

- 1.1「真实 AI 文本总结版」三小步已完成并冻结，线上闭环已验收。  
- 需要将 1.2 从「仅备忘」升级为「可执行启动准备」，并统一 `02/03/06` 的状态口径。

**结论**

- **1.2 PRD 已完成**：`docs/PRD/Read2Action_1.2_解析记录暂存与归档体验优化版_PRD.md`。  
- **1.2 启动方案已创建**：`docs/版本启动方案/08_1.2解析记录暂存与归档体验优化启动方案.md`。  
- **1.2 版本目标**：补全解析记录的暂存、归档、找回与真实空状态体验。  
- **开发策略**：采用分阶段推进，不一次性落完整 1.2。  
- **当前下一步**：进入 **1.2-A：真实空状态 + mock 不默认展示**。  
- **边界重申**：不接数据库、不做登录、不做文件上传、不做链接抓取、不做多模型 Provider。  
- **稳定性约束**：不破坏 `/parsing` 防重复请求机制 **A+B+C**，不重写 `/api/analyze`。

**文档动作**

- 同步更新 `02_当前进度快照.md`、`03_下一步任务清单.md`、`06_版本更新记录.md` 的阶段状态与下一步口径。  
- 本次为文档同步动作，不涉及业务代码改动。

---

## 2026-05-13 · 1.1「真实 AI 文本总结版」：**GitHub 推送 · Vercel 配置与 Production 部署 · 线上验收通过**（**总复盘文档已形成 · 完全冻结待确认**）

**结论**

- **Read2Action 1.1**：**本地三小步闭环** + **GitHub**（**`624428c`** `feat(1.1): complete real AI analyze and local save loop`）+ **Vercel** 环境变量与 **Production** 重部署后，**线上真实 AI 解析 + `localStorage` 手动保存闭环** **✅ 验收通过**；**保存仍为浏览器 `localStorage`**，属 **1.1 明确边界**（**不接库**）。  
- **阶段口径**：**1.1 功能与线上验收 ✅**；**正式总复盘文档**见 **`docs/阶段复盘/Read2Action_1.1_真实AI文本总结版总复盘.md`**；**1.1 完全阶段冻结**待审阅确认；**未**进入 **1.2**。详 **`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**（**一～六**全文）。

**线上验收摘要**

- **站点**：`https://read2action.vercel.app`；部署 **Ready / Production / Current**。  
- **Network**：过滤 **analyze** 后主路径 **1 条** **`POST /api/analyze`**、**200**、**fetch**；进入 **`/result`** 展示 **真实 AI**；「保存到项目」→ Toast → 侧栏「最近」/项目页/详情/双刷新 **不丢**；**mock** 笔记仍正常；**未**发现重复请求、保存失败、404、**undefined** 或异常 mock 冒充。  
- **结论句**：**1.1 线上真实 AI 解析链路 + 本地保存闭环通过。**

**环境变量与安全（Vercel + 本地）**

- Vercel：**`AI_API_KEY`**（新 DeepSeek Key；**Production + Preview**；**Sensitive**）、**`AI_BASE_URL`**=`https://api.deepseek.com`、**`AI_MODEL`**=`deepseek-chat`。代码读取 **`process.env.AI_API_KEY` / `AI_BASE_URL` / `AI_MODEL`**（**非** `DEEPSEEK_API_KEY`）。  
- **密钥不入库**；**`.env.local`** 已换 **新 Key**（**文档不写明文**）；**`.gitignore`** 忽略 **`.env.local`**；**禁止** `git add -f .env.local`。

**本地工程复验（线上验收后）**

- **`npm run test`**：**4** files、**26** tests **passed**（Vitest **`environmentMatchGlobs` deprecated** 提示保留，**不阻断**）。  
- **`npm run lint`**：**通过**。  
- **`npm run build`**：**Next 16.2.6** **通过**。

**P0 复盘提醒（写入 1.1 总复盘清单）**

- **`/parsing`** 曾 **重复 `POST /api/analyze`** → **费用 P0**；根因涉及 **StrictMode**、**cleanup/abort 与 `inFlightRef`**、**串行重复消费 pending 文本**、**定时器/阶段机间接触发**、**重挂载** 等；**最终保留 A+B+C**；验收标准为 **单次输入仅 1 条 POST（未点重试）**；须在 **1.1 总复盘** 写清 **根因、修复、费用风控、Network 标准、测试覆盖**。详 **`07` §2.6**、**`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md` §五**。

**1.1 后续剩余事项（流程）**

1. **提交** docs 收尾更新；2. **1.1 总复盘**；3. **复盘通过后冻结 1.1**；4. **再**进入 **1.2** 小 PRD。**1.2** 方向备忘：**「解析来源与自动暂存 / 自动归档机制」**（**`03`** / **`04`**，**当前不实现**）。

**文档动作**

- 新增 **`docs/阶段复盘/Read2Action_1.1_上线与验收收尾记录.md`**；同步 **`01` / `02` / `03` / `06` / `07` / `08`**。**业务代码**：本条 **仅文档**。

---

## 2026-05-13 · 1.1 第三小阶段「本地保存与最近记录最小闭环」：**最终人工验收通过 · 正式完成并冻结**

**结论**

- **1.1 第三小阶段**（**`localStorage`（`r2a:localSavedNotes`）+ `/result` 手动保存到项目** + **mock 合并**）经 **最终人工验收**，**✅ 正式完成**；本小阶段在 **产品 / 工程口径上正式冻结**（**不再**将本小步列为「待开发 / 待验收」开放项；若需调整须 **另起立项或变更单**，避免静默 **scope creep**）。  
- **与下文关系**：同日 **「第三小阶段收尾交付」** 条保留 **入库当日** 的工程摘要与决策表述；本条为 **验收结论固化 + 冻结声明**（真值以本条为准）。

**最终人工验收结论（✅ 全部通过）**

1. **首页**输入 → **`/parsing`** → **`/api/analyze`** → **`/result`** 链路正常。  
2. **Network** 过滤 **analyze** 后主路径 **仅 1 条** **`POST /api/analyze`**，**Status 200**。  
3. **`/result`** 正常展示 **真实 AI** 结构化结果。  
4. 点「**保存到项目**」后 **Toast**「**已保存到项目**」。  
5. 侧栏「**最近**」能显示保存后的 **本地** 记录。  
6. **项目页** 列表能显示保存后的 **本地** 记录。  
7. 点击 **本地保存** 记录能进入 **详情页**。  
8. **刷新详情页**后记录 **不丢失**。  
9. **刷新项目页**后记录 **不丢失**。  
10. 原有 **mock** 笔记链接 **仍能打开**。  
11. **`npm run test` / `npm run lint` / `npm run build`** **通过**。

**补充记录（已知体验，不阻断 1.1 本小阶段收尾）**

- **侧栏「最近」**：整页 **刷新瞬间**，**`localStorage`** 需在 **客户端 ready** 后再与 **mock** 合并；可能出现 **短暂**显示 **mock / 默认「最近」** 的加载态，属 **hydration / 合并时机** 的体验问题，**不阻断** **1.1 第三小阶段** **收尾验收**。后续可在 **1.2** 或独立 **体验优化** 立项中统一处理 **loading / skeleton / 无闪** 等细节。

**明确边界（1.1 冻结重申）**

1. **1.1** **不做** **自动暂存 / 自动归档**。  
2. **1.1** **不接数据库**。  
3. **1.1** **不接登录**。  
4. **1.1** **不做** **统一多模型 Provider** 架构（多模型方向 **仅备忘**，见本条之下 **「统一 AI Provider」** 备忘）。  
5. **1.1** **不改** **`/parsing` 防重复请求 A+B+C**（与 **`/api/analyze`** 主链路一并视为 **第二～三小阶段**已冻结的 **稳定面**）。  
6. **`1.2`** **后续另立项** 设计并实现 **「解析来源与自动暂存 / 自动归档机制」**（见 **`03`** 备忘与本文件 **「1.2 解析来源…」** 备忘条）。

**文档动作**

- 同步 **`01`** 文首状态、**`02`**（文首 / §1 / §1.5 / §1.6 / §8.4 / §9）、**`03`**（§0、本轮进度、第三小阶段 **交付收尾**）、**`06`**（时间线 / 里程碑 / 决策索引）、**`08`** 文首接力。**业务代码**：本条 **仅文档**，未改。

---

## 2026-05-13 · 备忘：多模型「统一 AI Provider」方向（**仅记录 · 未立项 · 本阶段不实现**）

**背景**

- 产品侧预期后续接入 **多家模型**（如 DeepSeek、通义、豆包、OpenAI 等），希望工程上 **避免「每接一家重写整条链路」**。

**方向（非承诺、非当前范围）**

- 前端统一传 **`text` + `modelProvider` + `modelId`**，仍经 **`POST /api/analyze`**；服务端 **按 provider 分发适配器**，响应 **统一 normalize** 为 **`ParseResultPreview` / `Note`** 所需结构；**`/result` 与保存逻辑** 尽量 **与具体模型解耦**。

**边界**

- **不纳入 1.1 默认排期**（**第三小阶段已交付并冻结**后）；**不**在未单独立项前改动 **`/parsing` → `/api/analyze` → `/result` → 手动保存`** 主链路与 **A+B+C** 防重复策略。  
- 详细备忘与立项入口见 **`03`**「**后续规划备忘：统一 AI Provider 架构**」。

---

## 2026-05-13 · 备忘：**1.2 解析来源与自动暂存 / 自动归档机制**（**历史口径：当日仅记录；现已进入 1.2 启动准备**）

**背景**

- **1.1 第三小阶段** 实现的是 **`/result` 手动保存到项目** 的最小闭环；**稳定**，但若用户从 **全局新笔记入口** 解析后 **未点保存**，存在 **「找不到这次解析」** 的产品风险。

**方向（非承诺、非当前范围）**

- 将解析入口拆为 **全局新笔记** vs **项目内解析**；引入 **`sourceContext`（如 global / project）**、**`savedStatus` / draft / temporary`** 等概念；全局路径下补充 **解析历史 / 暂存池 / 未归档**（命名待定）；项目内路径下 **自动归档到当前 `projectId`**，免二次选项目。

**边界**

- **1.1 第三小阶段 ✅ 已交付并冻结**后，本条仍 **不** 在未立项前改动 **`/parsing` → `/api/analyze` → `/result` → 手动保存`** 与 **A+B+C**。  
- 详细备忘与立项入口见 **`03`**「**后续规划备忘：1.2 解析来源与自动暂存 / 自动归档机制**」。

**当前结论（文档口径）**

- **1.1 第三小阶段**：**✅ 已按** **手动保存到项目** 收尾，**不并入**本条机制；**✅ 正式完成并冻结**（见 **`04` 文首**「**正式完成并冻结**」专条）。  
- **1.2**：单独设计并实现 **「解析来源与自动暂存 / 自动归档机制」**。

---

## 2026-05-13 · **1.1 第三小阶段收尾交付**与**产品决策**（**手动保存闭环 ✅**；**1.2 自动暂存/归档 不实现**）

**结论**

- **冻结与最终人工验收**：见 **本文件置顶** **「2026-05-13 · 1.1 第三小阶段…正式完成并冻结」** 专条。以下为 **收尾交付入库** 当日 **工程摘要**（仍有效）。  
- **1.1 第三小阶段**：**`localStorage`（`r2a:localSavedNotes`）+ `/result` 手动保存到项目** + **mock 合并**（侧栏「最近」、项目列表、**`NoteDetailResolver`** 详情）**✅ 已收尾**；工程 **`npm run test`（26）**、**`npm run lint`**、**`npm run build`** 通过；变更与人工 **Checklist** 见 **`03`**「**1.1 第三小阶段 · 交付收尾**」、**`02` §8.4**。  
- **产品决策**：**不**在本阶段实现 **「解析完成自动暂存 / 自动归档」**（全局暂存池、项目内自动入库等）；记入 **1.2** 待办（见上条与本日 **「统一 AI Provider」** 备忘）。  
- **工程约束**：**未**改 **`parsing-page-view.tsx`** 防重复调度、**未**改 **`/api/analyze`** 核心。

**文档动作**

- 同步 **`02` §1.6、§8.4**；**`03`** §0、第三小阶段与「**交付收尾**」、**1.2** 备忘首行 **产品决策**；**`06`** 时间线；**`08`** 文首接力说明。

---

## 2026-05-21 · 新对话接力节点：1.1 第二小阶段结论固化；第三小阶段暂停进入实现

**背景**

- **1.1 第一、二小阶段** 已按 **`08` §0** 与 **`02` §8.2～§8.3** 落地；第二小阶段经 **Network + webpack dev** 复验通过。  
- 排障过程中 **P0** 经历 **0 条 POST（pending 误清）**、**多条 POST（费用风险）**、**单条 pending（与 Turbopack FATAL 干扰叠加）** 等阶段；最终以 **A+B+C** + **`next dev --webpack`** + **清 `.next` / 单进程** 收束（全过程见 **`02` §1.6 二、三**）。  
- 用户要求 **暂停进入 1.1 第三小阶段**，先将进度写入 **docs** 作为 **新对话接力节点**。

**结论**

- **1.0**：冻结不变。  
- **1.1 第二小阶段**：真实 AI 主链路 **已接通且已通过复验**；人工与工程验收摘要见 **`02` §1.6 三**（含 **~6.37s** 等一次复验数据、**test/lint/build** 与备注）。  
- **1.1 第三小阶段**（**2026-05-21 当时表述**：**仅预备 / 未开工**）— **已由 2026-05-13 文档修订与代码落地关闭**；**当前真值**见 **`02` §8.4**、**`03`**「**1.1 第三小阶段 · 交付收尾**」、**`04` 2026-05-13** 文首 **「正式完成并冻结」** 专条（及同日 **「第三小阶段收尾交付」**）。本条 **2026-05-21** 保留为 **当时接力节点** 的历史记录。

**文档动作**

- **（2026-05-21 当时）** 新增 / 更新：**`02` §1.6、§8.4**；**`03`** 默认立场与第三小阶段预备节；**`06` 2026-05-21**；**`07` §2.5**；**`08` 文首接力说明**；**`01`** 当前状态行。  
- **（2026-05-13 修订）** 第三小阶段 **已落地** 后，同步 **`02` §1.6 四～五、§8.4**；**`03`** §0、第三小阶段与「**交付收尾**」、**1.2** 备忘；**`06` 2026-05-13** 行、**`01` / `07` / `08`** 进度表述；**`04` 2026-05-13**「**第三小阶段收尾交付**」。**（验收冻结）** 见 **`04` 2026-05-13** 文首 **「正式完成并冻结」**。

---

## 2026-05-20 · 1.1 第二小阶段 P0 收束：webpack dev、人工复验结论与文档同步

**背景**

- **原始 P0**：**`/parsing` 重复请求 `POST /api/analyze`**，存在 **DeepSeek（上游）费用风险**；已在前序迭代以 **A+B+C** 防重复方案与代码修复为主轴处理（见 **`06` 2026-05-19**、**`07` §2.4**）。  
- **中间干扰**：**Next.js 16** 下 **`next dev` 无显式 flag 时默认使用 Turbopack**；本地 dev 曾出现 **Turbopack FATAL**，**dev server 不稳定**，干扰 **`/api/analyze` 与 `/parsing`→`/result` 的人工复验**（易与「上游长时间 pending」混淆）。  
- **收束动作**：将 **`package.json`** 的 **`dev`** 固定为 **`next dev --webpack`**；**停止重复 dev 进程**、**删除 `.next`** 后重启 **`npm run dev`**，再按 Network 标准复验。

**结论**

- **业务侧**：**保留 A+B+C 防重复请求方案**，**不推翻**前序最小改与测试策略。  
- **本地 dev 约定**：**1.1 第二小阶段**相关人工复验，以 **`npm run dev`**（即 **`next dev --webpack`**）为 **推荐基线**，避免默认 Turbopack 干扰排障。  
- **人工复验（webpack dev）已通过**：Chrome DevTools **Network** 过滤 **analyze** 后 **仅 1 条 `POST /api/analyze`**、**Status 200**、进入 **`/result`** 且展示 **真实 AI 总结**；终端 **未再出现** Turbopack **FATAL**。  
- **工程收束**：**`npm run test`、`npm run lint`、`npm run build`** 均已通过（与 **`06` 2026-05-20** 行一致）。  
- **阶段边界**：**不默认进入**下一小步（如保存链路等）；仍须 **`08` / `03`** 单独立项后再扩代码。

**文档动作**

- 更新 **`06_版本更新记录.md`**（时间线 + 里程碑）、**`02_当前进度快照.md`**、**`03_下一步任务清单.md`**、**`07_方法论沉淀.md` §2.4**、本文件本条。

---

## 2026-05-16 · 明确 Read2Action 1.1 为「真实 AI 文本总结版」及范围边界（防 scope 失控）

**背景**

- 1.0 已上线并冻结，团队准备进入 **1.1 真实 AI** 能力；若不在文档中 **预先收窄范围**，易出现 **「顺便做链接抓取 / 顺便做账号与数据库」** 等 **scope 失控**，把 1.1 拖成小 2.0。  
- 需要把 **1.1 的产品定义** 与 **明确不做清单** 写入 **`02_当前进度快照.md`、`03_下一步任务清单.md`、本日志**，供立项、排期与 Cursor 接力统一引用。

**结论**

- **Read2Action 1.1** 定义为 **「真实 AI 文本总结版」**。  
- **1.1 只做**：用户输入 **一段文本** → **调用 AI** → 返回 **结构化总结** → **`/result` 等结果路径展示真实生成内容**（在既有六页主流程与信息架构下，将核心体验从 mock 升级为真实模型输出）。  
- **1.1 暂不做**：**链接全文抓取**；**登录注册**；**数据库**；**多用户**；**文件上传**；**浏览器插件**；**复杂知识库**；**方法论自动生成**。  
- **链接输入策略**：若用户输入 **链接 / URL**，**1.1 不实现**自动读取正文；**仅允许轻量提示**，建议文案口径：**「当前版本暂不支持自动读取链接正文，请复制正文后粘贴。」**  
- **边界目的**：**避免 1.1 范围失控**；**先把产品从 mock 演示变成真实可用**（优先跑通 **文本进 → 真 AI 出 → 结果页真展示**）。

**文档动作**

- 更新 `02_当前进度快照.md` **§8**、`03_下一步任务清单.md`（新增 **「Read2Action 1.1 范围边界」** 专节并同步 §0/§1/§3/§4）、本文件本条。

---

## 2026-05-15 · Read2Action 1.0 正式上线：第十阶段闭环（10.1 push / 10.2 Vercel）与 1.1 准备期

**背景**

- **Read2Action 1.0** 已完成 **GitHub 首次上传** 与 **Vercel 生产部署**；**线上一致主流程** 已 **人工验收通过**：首页输入 → 开始解析 → **`/parsing`** → **`/result`** → 保存到项目 → **`/projects/sha`** → 笔记详情。  
- **线上仍为 mock + 本地/前端状态**，为 **1.0 预期**，**不视为 bug**。  
- **10.2**「Vercel 导入 GitHub 仓库并部署」标记为 **已完成**。

**结论**

- **Read2Action 1.0** 可 **正式标记为完成** 并 **冻结**（功能与视觉不再演进，除非单独立项新版本）。  
- **第十阶段**（真实可用性检查与上线准备）**闭环**：**10.0**、**10.1**、**10.2** 均 **已完成**。  
- **线上地址**：`https://read2action.vercel.app`；**GitHub 仓库**：`https://github.com/shamingxin/read2action`。  
- **后续默认阶段**：**1.1「真实 AI 接入」— 准备期**；在未明确立项与范围前，**不修改业务代码、不新增功能、不做视觉迭代**（与对话约定一致）。

**文档动作**

- 更新进度快照、接力文档、开发交接总文档 §16 / §17，写入线上与仓库地址、已完成阶段与 1.1 准备期边界。  
- **体系重构**：上述内容已归并至 `01`～`07` 新文档。

---

## 2026-05-14 · 第十阶段 10.0 通过 & 10.1 GitHub 仓库就绪（未 push）

> **注（2026-05-15）**：本条目记录 **push 前** 节点；**10.1 push** 与 **10.2 Vercel 部署** 的完成态见 **2026-05-15** 条目。

**背景**

- **10.0** 本地部署前检查：**`npm run lint`**、**`npm run build`** 已通过；**`npm run dev`** 下主要路由与主流程 **基本正常**。  
- **10.1**：准备 **GitHub + Vercel** 部署；已在 GitHub 创建 **空仓库**，本地项目 **当时尚未** 推送。

**结论**

- **10.0**：**通过**（以人工复验结论为准）。  
- **10.1（本日节点）**：远程空仓库已创建：`https://github.com/shamingxin/read2action.git`；后续由本地完成 **Git 初始化 / 首次 commit / push** 与 **Vercel** 关联部署（完成记录见 **2026-05-15**）。  
- **本节点**：**不**在当次对话执行 push、**不**改业务代码与视觉（与对话约定一致）。

**文档动作**

- 更新进度快照、接力文档、开发交接总文档 §16，记录 10.0/10.1 状态与远程地址。

---

## 2026-05-13 · 产品版本路线补充（1.0 / 1.1 / 2.0）

**背景**

- 需在文档中固化 **1.0 之后** 的产品方向，避免与「当前不做」混淆；**1.0** 仍以 **mock + 本地状态稳定上线** 为主。

**结论**

- **1.0**：不变 — mock + 本地状态，**稳定上线**；第十阶段聚焦可用性与部署准备。  
- **1.1（后续计划）**：接入 **真实 AI** — 新增 **`/api/analyze`**、后端调用大模型、用户输入 → 结构化结果、**结果页替换 mock**；**保留 `/parsing` 四步体验**；**仍不做**登录、数据库、真实删除、真实导出（与规划一致）。  
- **2.0（再考虑）**：数据库保存项目/笔记、登录注册、多项目真实管理、历史记录、搜索、导出、多端同步等。

**文档动作**

- 接力文档 §1 增补「版本路线」、厘清 **1.0「当前不做」** 与 **1.1** 边界；开发交接总文档新增 **§17**；进度快照增加交叉引用。

---

## 2026-05-12 · 第九阶段 9.3 通过验收与第十阶段立项

**背景**

- 1.0 **mock 主流程**与 **9.3 收尾修复**已完成；**人工复验**暂未发现明显大问题。  
- 目标从「走查 + 极小修复」过渡到：**把产品当真实可用小产品**，做 **上线前稳定性、实用性、部署准备**。

**结论**

- **9.3**（主流程完整走查 + 收尾 + 复验）：**标记为通过**。  
- **第十阶段**正式立项：**真实可用性检查与上线准备**（**非**以作品集包装为首要目标）。

**9.3 闭环要点（摘要）**

- 主链路 **`/` → `/parsing` → `/result` → 保存到项目 → `/projects/sha` → 笔记详情** 可跑通。  
- **`/parsing`**：四步完整展示后再进 **`/result`**；取消解析无幽灵跳转。  
- **`/result` 与详情**「编辑」→ Toast；Sidebar「新增项目」仅 Toast；项目 **⋯** / **active 选中态** / **行高** / **icon** 等小修已合入。  
- **`npm run build`** 已通过。

**第十阶段边界（暂不做）**

- 不接 **真实 AI API**；不接 **数据库**；不做 **登录注册**。  
- 不做 **真实删除 / 重命名 / 分享**；不做 **2.0 视觉大改**。  
- **不重新讨论**产品方向与 **页面结构**。

**文档动作**

- 更新 9.3 问题记录表（阶段结论与问题闭环）、进度快照、接力文档、开发交接总文档 §16。  
- **体系重构**：9.3 明细已并入 **`07_方法论沉淀.md`**。

---

## 2026-05-11 · 第九阶段 9.1 / 9.2 验收与文档同步

**结论**

- **9.1**（全局组件统一 + 一轮小修）：**验收通过**。  
- **9.2**（页面视觉统一 + 收尾交互）：**验收通过**。  
- **9.3**（主流程完整走查）：**仅预备进入**；本日期当日对话以 **文档更新** 为范围，**不**开工 9.3 实现与走查执行。

**9.2 已纳入验收的视觉与交互要点（摘要）**

- **1020 内容体系**：`r2aPageShell1020` 与各页 `SectionCard` / Tab / 顶栏 token 统一；`/parsing` LIVE 卡阴影与全站 `r2aCardShadow` 一致。  
- **首页**：1020 外壳 + 内层约 720 输入列；解析按钮置灰与 Toast 行为保持 9.1 逻辑。  
- **详情「⋯」**：与 `/result` 同款 Dropdown；`z-20` + `shrink-0` 修复点击与叠层问题。  
- **占位反馈**：首页「+」、结果页「添加自定义卡片」→ 明确 Toast 文案；详情不新增「添加自定义卡片」按钮以免改布局。

**范围决策**

- **当前不继续开发新功能**，**不进入 9.3 执行**，直至新对话以 **9.3 走查** 为明确目标。  
- **9.3 边界**：走查与问题清单为主；不接真实 API、不改 mock 数据、不大改布局；像素级 Figma、`/result` 原文对照真实内容、方法论占位替换等 **不默认**纳入 9.3。

**文档动作**

- 更新接力文档、进度快照 **v0.8**、本决策日志、开发交接总文档中的 **第九阶段交接摘要**，供下一 Cursor 对话接力。

---

## 模板（后续追加条目时使用）

```text
## YYYY-MM-DD · 标题

**背景**：
**选项与取舍**：
**结论**：
**影响范围（页面 / 文件 / 文档）**：
```

---

**文档路径**：`docs/核心记录/04_重要决策日志.md`
